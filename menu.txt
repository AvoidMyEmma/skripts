-- LocalScript inside StarterPlayerScripts or StarterCharacterScripts
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local localPlayer = Players.LocalPlayer
local camera = Workspace.CurrentCamera

-- Clear existing UI instances
local existingGui = localPlayer:WaitForChild("PlayerGui"):FindFirstChild("ModMenuGui")
if existingGui then
	existingGui:Destroy()
end

---------------------------------------------------------
-- TOGGLE STATES & CONFIGURATION
---------------------------------------------------------
local aimbotEnabled = false
local espEnabled = false
local flyEnabled = false
local noclipEnabled = false
local speedMultiplier = 1 -- 1x multiplier (default)

local HIGHLIGHT_COLOR = Color3.fromRGB(255, 0, 0)[cite: 1]
local OUTLINE_COLOR = Color3.fromRGB(255, 255, 255)[cite: 1]
local FILL_TRANSPARENCY = 0.5[cite: 1]
local OUTLINE_TRANSPARENCY = 0[cite: 1]

---------------------------------------------------------
-- UI CREATION
---------------------------------------------------------
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ModMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = localPlayer:WaitForChild("PlayerGui")[cite: 1]

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 260, 0, 560)
mainFrame.Position = UDim2.new(0, 20, 0.5, -280)
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 42)[cite: 1]
mainFrame.BorderSizePixel = 0[cite: 1]
mainFrame.Active = true[cite: 1]
mainFrame.Draggable = true[cite: 1]
mainFrame.Parent = screenGui[cite: 1]

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 8)[cite: 1]
uiCorner.Parent = mainFrame[cite: 1]

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -30, 0, 36)
titleLabel.Position = UDim2.new(0, 10, 0, 0)[cite: 1]
titleLabel.BackgroundTransparency = 1[cite: 1]
titleLabel.Text = "Mod Menu (RightCtrl)"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)[cite: 1]
titleLabel.TextSize = 14
titleLabel.TextXAlignment = Enum.TextXAlignment.Left[cite: 1]
titleLabel.Font = Enum.Font.SourceSansBold[cite: 1]
titleLabel.Parent = mainFrame[cite: 1]

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"[cite: 1]
closeButton.Size = UDim2.new(0, 24, 0, 24)[cite: 1]
closeButton.Position = UDim2.new(1, -29, 0, 6)
closeButton.BackgroundColor3 = Color3.fromRGB(220, 60, 60)[cite: 1]
closeButton.Text = "X"[cite: 1]
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)[cite: 1]
closeButton.TextSize = 13[cite: 1]
closeButton.Font = Enum.Font.SourceSansBold[cite: 1]
closeButton.Parent = mainFrame[cite: 1]

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)[cite: 1]
closeCorner.Parent = closeButton[cite: 1]

local function createButton(text, pos, bgCol)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -20, 0, 30)
	btn.Position = pos
	btn.BackgroundColor3 = bgCol
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)[cite: 1]
	btn.TextSize = 13
	btn.Font = Enum.Font.SourceSansBold[cite: 1]
	btn.Parent = mainFrame
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)[cite: 1]
	corner.Parent = btn
	return btn
end

-- Toggle Buttons
local aimbotToggleBtn = createButton("Aimbot: OFF", UDim2.new(0, 10, 0, 40), Color3.fromRGB(180, 50, 50))
local espToggleBtn    = createButton("ESP Highlights: OFF", UDim2.new(0, 10, 0, 75), Color3.fromRGB(180, 50, 50))
local flyToggleBtn    = createButton("Fly: OFF", UDim2.new(0, 10, 0, 110), Color3.fromRGB(180, 50, 50))
local noclipToggleBtn = createButton("Noclip: OFF", UDim2.new(0, 10, 0, 145), Color3.fromRGB(180, 50, 50))

---------------------------------------------------------
-- SPEED MULTIPLIER SLIDER UI
---------------------------------------------------------
local sliderFrame = Instance.new("Frame")
sliderFrame.Size = UDim2.new(1, -20, 0, 45)
sliderFrame.Position = UDim2.new(0, 10, 0, 180)
sliderFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
sliderFrame.Parent = mainFrame

local sliderCorner = Instance.new("UICorner")
sliderCorner.CornerRadius = UDim.new(0, 6)[cite: 1]
sliderCorner.Parent = sliderFrame

local sliderLabel = Instance.new("TextLabel")
sliderLabel.Size = UDim2.new(1, -10, 0, 20)
sliderLabel.Position = UDim2.new(0, 5, 0, 2)
sliderLabel.BackgroundTransparency = 1[cite: 1]
sliderLabel.Text = "Speed Multiplier: 1.0x"
sliderLabel.TextColor3 = Color3.fromRGB(255, 255, 255)[cite: 1]
sliderLabel.TextSize = 12
sliderLabel.Font = Enum.Font.SourceSansBold[cite: 1]
sliderLabel.Parent = sliderFrame

local sliderBar = Instance.new("Frame")
sliderBar.Size = UDim2.new(1, -20, 0, 8)
sliderBar.Position = UDim2.new(0, 10, 0, 26)
sliderBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
sliderBar.Parent = sliderFrame

local sliderFill = Instance.new("Frame")
sliderFill.Size = UDim2.new(0, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
sliderFill.BorderSizePixel = 0
sliderFill.Parent = sliderBar

local sliderDragging = false
local MIN_MULT, MAX_MULT = 1, 10 -- 1x to 10x Speed

local function applySpeedMultiplier()
	if localPlayer.Character then
		local humanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")[cite: 1]
		if humanoid then
			-- Calculate base speed (default 16 or current base) and multiply
			local baseSpeed = humanoid:GetAttribute("BaseWalkSpeed") or 16
			humanoid.WalkSpeed = baseSpeed * speedMultiplier
		end
	end
end

local function updateSlider(input)
	local barPos = sliderBar.AbsolutePosition.X
	local barSize = sliderBar.AbsoluteSize.X
	local mousePos = input.Position.X
	local percentage = math.clamp((mousePos - barPos) / barSize, 0, 1)

	sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
	-- Format to 1 decimal place (e.g., 2.5x)
	speedMultiplier = math.floor((MIN_MULT + (percentage * (MAX_MULT - MIN_MULT))) * 10) / 10
	sliderLabel.Text = "Speed Multiplier: " .. string.format("%.1fx", speedMultiplier)

	applySpeedMultiplier()
end

sliderBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		sliderDragging = true
		updateSlider(input)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		sliderDragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if sliderDragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		updateSlider(input)
	end
end)

---------------------------------------------------------
-- TELEPORT UI
---------------------------------------------------------
local scrollFrame = Instance.new("ScrollingFrame")[cite: 1]
scrollFrame.Size = UDim2.new(1, -20, 0, 150)
scrollFrame.Position = UDim2.new(0, 10, 0, 235)
scrollFrame.BackgroundTransparency = 1[cite: 1]
scrollFrame.ScrollBarThickness = 4[cite: 1]
scrollFrame.Parent = mainFrame[cite: 1]

local uiListLayout = Instance.new("UIListLayout")[cite: 1]
uiListLayout.SortOrder = Enum.SortOrder.LayoutOrder[cite: 1]
uiListLayout.Padding = UDim.new(0, 4)[cite: 1]
uiListLayout.Parent = scrollFrame[cite: 1]

local tpButton = createButton("Teleport to Selected Player", UDim2.new(0, 10, 1, -44), Color3.fromRGB(0, 162, 255))[cite: 1]

---------------------------------------------------------
-- AIMBOT SYSTEM
---------------------------------------------------------
local function getNearestHead()
	local localCharacter = localPlayer.Character[cite: 1]
	if not localCharacter or not localCharacter:FindFirstChild("HumanoidRootPart") then return nil end[cite: 1]

	local localPosition = localCharacter.HumanoidRootPart.Position[cite: 1]
	local nearestHead = nil[cite: 1]
	local shortestDistance = math.huge[cite: 1]

	for _, player in ipairs(Players:GetPlayers()) do[cite: 1]
		if player ~= localPlayer and player.Character then[cite: 1]
			local targetHead = player.Character:FindFirstChild("Head")[cite: 1]
			local targetHumanoid = player.Character:FindFirstChildOfClass("Humanoid")[cite: 1]
			
			if targetHead and targetHumanoid and targetHumanoid.Health > 0 then[cite: 1]
				local distance = (targetHead.Position - localPosition).Magnitude[cite: 1]
				if distance < shortestDistance then[cite: 1]
					shortestDistance = distance[cite: 1]
					nearestHead = targetHead[cite: 1]
				end
			end
		end
	end
	return nearestHead[cite: 1]
end

RunService.RenderStepped:Connect(function()
	if not aimbotEnabled then return end

	local character = localPlayer.Character[cite: 1]
	if not character then return end[cite: 1]
	
	local humanoid = character:FindFirstChildOfClass("Humanoid")[cite: 1]
	if not humanoid or humanoid.Health <= 0 then return end[cite: 1]

	local targetHead = getNearestHead()[cite: 1]
	if targetHead then[cite: 1]
		local currentCamPos = camera.CFrame.Position[cite: 1]
		camera.CameraType = Enum.CameraType.Scriptable[cite: 1]
		camera.CFrame = CFrame.new(currentCamPos, targetHead.Position)[cite: 1]
	else
		if camera.CameraType ~= Enum.CameraType.Custom then[cite: 1]
			camera.CameraType = Enum.CameraType.Custom[cite: 1]
		end
	end
end)

aimbotToggleBtn.MouseButton1Click:Connect(function()
	aimbotEnabled = not aimbotEnabled
	if aimbotEnabled then
		aimbotToggleBtn.Text = "Aimbot: ON"
		aimbotToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		aimbotToggleBtn.Text = "Aimbot: OFF"
		aimbotToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
		camera.CameraType = Enum.CameraType.Custom[cite: 1]
	end
end)

---------------------------------------------------------
-- ESP HIGHLIGHT SYSTEM
---------------------------------------------------------
local function applyHighlight(character)
	if character == localPlayer.Character then return end[cite: 1]
	
	local existingHighlight = character:FindFirstChild("PlayerHighlight")[cite: 1]
	if espEnabled then
		if not existingHighlight then[cite: 1]
			local highlight = Instance.new("Highlight")[cite: 1]
			highlight.Name = "PlayerHighlight"[cite: 1]
			highlight.FillColor = HIGHLIGHT_COLOR[cite: 1]
			highlight.OutlineColor = OUTLINE_COLOR[cite: 1]
			highlight.FillTransparency = FILL_TRANSPARENCY[cite: 1]
			highlight.OutlineTransparency = OUTLINE_TRANSPARENCY[cite: 1]
			highlight.Adornee = character[cite: 1]
			highlight.Parent = character[cite: 1]
		end
	else
		if existingHighlight then
			existingHighlight:Destroy()[cite: 1]
		end
	end
end

local function updateAllHighlights()
	for _, player in ipairs(Players:GetPlayers()) do[cite: 1]
		if player.Character then[cite: 1]
			applyHighlight(player.Character)[cite: 1]
		end
	end
end

local function onPlayerAdded(player)
	if player == localPlayer then return end[cite: 1]
	if player.Character then applyHighlight(player.Character) end[cite: 1]
	player.CharacterAdded:Connect(applyHighlight)[cite: 1]
end

for _, player in ipairs(Players:GetPlayers()) do onPlayerAdded(player) end[cite: 1]
Players.PlayerAdded:Connect(onPlayerAdded)[cite: 1]

espToggleBtn.MouseButton1Click:Connect(function()
	espEnabled = not espEnabled
	if espEnabled then
		espToggleBtn.Text = "ESP Highlights: ON"
		espToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		espToggleBtn.Text = "ESP Highlights: OFF"
		espToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
	updateAllHighlights()
end)

---------------------------------------------------------
-- FLY SYSTEM
---------------------------------------------------------
local bodyVelocity = nil
local bodyGyro = nil
local FLY_SPEED = 50

flyToggleBtn.MouseButton1Click:Connect(function()
	flyEnabled = not flyEnabled
	local character = localPlayer.Character
	local root = character and character:FindFirstChild("HumanoidRootPart")

	if flyEnabled then
		flyToggleBtn.Text = "Fly: ON"
		flyToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
		if root then
			bodyVelocity = Instance.new("BodyVelocity")
			bodyVelocity.Velocity = Vector3.zero
			bodyVelocity.MaxForce = Vector3.new(4e5, 4e5, 4e5)
			bodyVelocity.Parent = root

			bodyGyro = Instance.new("BodyGyro")
			bodyGyro.MaxTorque = Vector3.new(4e5, 4e5, 4e5)
			bodyGyro.CFrame = root.CFrame
			bodyGyro.Parent = root
		end
	else
		flyToggleBtn.Text = "Fly: OFF"
		flyToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
		if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
		if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
	end
end)

RunService.RenderStepped:Connect(function()
	if flyEnabled and bodyVelocity and bodyGyro then
		local moveDir = Vector3.zero
		if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camera.CFrame.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camera.CFrame.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camera.CFrame.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camera.CFrame.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
		if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end

		bodyVelocity.Velocity = moveDir.Magnitude > 0 and moveDir.Unit * (FLY_SPEED * speedMultiplier) or Vector3.zero
		bodyGyro.CFrame = camera.CFrame
	end
end)

---------------------------------------------------------
-- NOCLIP SYSTEM
---------------------------------------------------------
noclipToggleBtn.MouseButton1Click:Connect(function()
	noclipEnabled = not noclipEnabled
	if noclipEnabled then
		noclipToggleBtn.Text = "Noclip: ON"
		noclipToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		noclipToggleBtn.Text = "Noclip: OFF"
		noclipToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
end)

RunService.Stepped:Connect(function()
	if noclipEnabled and localPlayer.Character then
		for _, part in ipairs(localPlayer.Character:GetDescendants()) do
			if part:IsA("BasePart") then
				part.CanCollide = false
			end
		end
	end
end)

---------------------------------------------------------
-- TELEPORT SYSTEM
---------------------------------------------------------
local selectedPlayer = nil[cite: 1]

uiListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, uiListLayout.AbsoluteContentSize.Y)[cite: 1]
end)

local function refreshPlayerList()
	for _, child in ipairs(scrollFrame:GetChildren()) do[cite: 1]
		if child:IsA("TextButton") then child:Destroy() end[cite: 1]
	end

	for _, player in ipairs(Players:GetPlayers()) do[cite: 1]
		if player ~= localPlayer then[cite: 1]
			local pButton = Instance.new("TextButton")[cite: 1]
			pButton.Size = UDim2.new(1, -6, 0, 28)[cite: 1]
			pButton.BackgroundColor3 = (selectedPlayer == player) and Color3.fromRGB(0, 120, 215) or Color3.fromRGB(50, 50, 60)[cite: 1]
			pButton.Text = player.DisplayName .. " (@" .. player.Name .. ")"[cite: 1]
			pButton.TextColor3 = Color3.fromRGB(200, 200, 200)[cite: 1]
			pButton.TextSize = 13[cite: 1]
			pButton.Font = Enum.Font.SourceSans[cite: 1]
			pButton.Parent = scrollFrame[cite: 1]

			local pCorner = Instance.new("UICorner")[cite: 1]
			pCorner.CornerRadius = UDim.new(0, 4)[cite: 1]
			pCorner.Parent = pButton[cite: 1]

			pButton.MouseButton1Click:Connect(function()
				selectedPlayer = player[cite: 1]
				for _, btn in ipairs(scrollFrame:GetChildren()) do[cite: 1]
					if btn:IsA("TextButton") then
						btn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)[cite: 1]
					end
				end
				pButton.BackgroundColor3 = Color3.fromRGB(0, 120, 215)[cite: 1]
			end)
		end
	end
end

tpButton.MouseButton1Click:Connect(function()
	if not selectedPlayer then return end[cite: 1]

	local myChar = localPlayer.Character[cite: 1]
	local targetChar = selectedPlayer.Character[cite: 1]

	if myChar and targetChar then[cite: 1]
		local myRoot = myChar:FindFirstChild("HumanoidRootPart")[cite: 1]
		local targetRoot = targetChar:FindFirstChild("HumanoidRootPart")[cite: 1]

		if myRoot and targetRoot then[cite: 1]
			myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 3)[cite: 1]
		end
	end
end)

---------------------------------------------------------
-- UI TOGGLE & CHARACTER HANDLERS
---------------------------------------------------------
closeButton.MouseButton1Click:Connect(function()
	mainFrame.Visible = false[cite: 1]
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if not gameProcessed and input.KeyCode == Enum.KeyCode.RightControl then[cite: 1]
		mainFrame.Visible = not mainFrame.Visible[cite: 1]
	end
end)

local function onCharacterAdded(char)
	local humanoid = char:WaitForChild("Humanoid")
	-- Store original base speed if not set
	if not humanoid:GetAttribute("BaseWalkSpeed") then
		humanoid:SetAttribute("BaseWalkSpeed", humanoid.WalkSpeed)
	end
	applySpeedMultiplier()
end

if localPlayer.Character then
	onCharacterAdded(localPlayer.Character)
end

localPlayer.CharacterAdded:Connect(onCharacterAdded)

Players.PlayerAdded:Connect(refreshPlayerList)[cite: 1]
Players.PlayerRemoving:Connect(function(player)
	if selectedPlayer == player then selectedPlayer = nil end[cite: 1]
	refreshPlayerList()[cite: 1]
end)

refreshPlayerList()[cite: 1]
