-- LocalScript inside StarterPlayerScripts or StarterCharacterScripts
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")

local localPlayer = Players.LocalPlayer
local camera = Workspace.CurrentCamera

-- Clear existing UI instances
local existingGui = localPlayer:WaitForChild("PlayerGui"):FindFirstChild("ModMenuGui")
if existingGui then
	existingGui:Destroy()
end

---------------------------------------------------------
-- GLOBAL TOGGLE STATES & CONFIGURATION
---------------------------------------------------------
local aimbotEnabled = false
local espEnabled = false
local flyEnabled = false
local noclipEnabled = false
local speedMultiplier = 1
local flySpeedMultiplier = 1
local BASE_FLY_SPEED = 50

local HIGHLIGHT_COLOR = Color3.fromRGB(255, 0, 0)
local OUTLINE_COLOR = Color3.fromRGB(255, 255, 255)
local FILL_TRANSPARENCY = 0.5
local OUTLINE_TRANSPARENCY = 0

---------------------------------------------------------
-- MAIN UI CONTAINER
---------------------------------------------------------
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ModMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = localPlayer:WaitForChild("PlayerGui")

---------------------------------------------------------
-- INTRO / LOADING SCREEN ("ModMenu by Emma")
---------------------------------------------------------
local loadingFrame = Instance.new("Frame")
loadingFrame.Name = "LoadingFrame"
loadingFrame.Size = UDim2.new(0, 320, 0, 150)
loadingFrame.Position = UDim2.new(0.5, -160, 0.5, -75)
loadingFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
loadingFrame.BorderSizePixel = 0
loadingFrame.Active = true
loadingFrame.Parent = screenGui

local loadingCorner = Instance.new("UICorner")
loadingCorner.CornerRadius = UDim.new(0, 10)
loadingCorner.Parent = loadingFrame

local loadingTitle = Instance.new("TextLabel")
loadingTitle.Size = UDim2.new(1, 0, 0, 40)
loadingTitle.Position = UDim2.new(0, 0, 0, 15)
loadingTitle.BackgroundTransparency = 1
loadingTitle.Text = "ModMenu by Emma"
loadingTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
loadingTitle.TextSize = 20
loadingTitle.Font = Enum.Font.SourceSansBold
loadingTitle.Parent = loadingFrame

local loadingStatus = Instance.new("TextLabel")
loadingStatus.Size = UDim2.new(1, 0, 0, 20)
loadingStatus.Position = UDim2.new(0, 0, 0, 55)
loadingStatus.BackgroundTransparency = 1
loadingStatus.Text = "Initializing features..."
loadingStatus.TextColor3 = Color3.fromRGB(180, 180, 180)
loadingStatus.TextSize = 13
loadingStatus.Font = Enum.Font.SourceSans
loadingStatus.Parent = loadingFrame

local barBackground = Instance.new("Frame")
barBackground.Size = UDim2.new(1, -40, 0, 10)
barBackground.Position = UDim2.new(0, 20, 0, 95)
barBackground.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
barBackground.BorderSizePixel = 0
barBackground.Parent = loadingFrame

local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(0, 5)
barCorner.Parent = barBackground

local barFill = Instance.new("Frame")
barFill.Size = UDim2.new(0, 0, 1, 0)
barFill.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
barFill.BorderSizePixel = 0
barFill.Parent = barBackground

local fillCorner = Instance.new("UICorner")
fillCorner.CornerRadius = UDim.new(0, 5)
fillCorner.Parent = barFill

---------------------------------------------------------
-- MAIN MENU FRAME (HIDDEN INITIALLY)
---------------------------------------------------------
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 260, 0, 360)
mainFrame.Position = UDim2.new(0, 20, 0.5, -180)
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
mainFrame.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 8)
uiCorner.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -30, 0, 36)
titleLabel.Position = UDim2.new(0, 10, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Mod Menu (RightShift)"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 14
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.Parent = mainFrame

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 24, 0, 24)
closeButton.Position = UDim2.new(1, -29, 0, 6)
closeButton.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextSize = 13
closeButton.Font = Enum.Font.SourceSansBold
closeButton.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeButton

local function createButton(text, pos, bgCol, parent)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -20, 0, 30)
	btn.Position = pos
	btn.BackgroundColor3 = bgCol
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 13
	btn.Font = Enum.Font.SourceSansBold
	btn.Parent = parent or mainFrame
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = btn
	return btn
end

-- Feature Buttons inside Main Menu
local aimbotToggleBtn  = createButton("Aimbot: OFF", UDim2.new(0, 10, 0, 40), Color3.fromRGB(180, 50, 50))
local espToggleBtn     = createButton("ESP Highlights: OFF", UDim2.new(0, 10, 0, 75), Color3.fromRGB(180, 50, 50))
local flyToggleBtn     = createButton("Fly: OFF", UDim2.new(0, 10, 0, 110), Color3.fromRGB(180, 50, 50))
local noclipToggleBtn  = createButton("Noclip: OFF", UDim2.new(0, 10, 0, 145), Color3.fromRGB(180, 50, 50))
local toggleTpMenuBtn  = createButton("TP Menu: OFF", UDim2.new(0, 10, 0, 275), Color3.fromRGB(180, 50, 50))
local nightsBtn        = createButton("99 Nights: OFF", UDim2.new(0, 10, 0, 315), Color3.fromRGB(180, 50, 50))

---------------------------------------------------------
-- HELPER SLIDER CREATOR
---------------------------------------------------------
local function createSlider(yPos, defaultText, minVal, maxVal, onUpdate)
	local frame = Instance.new("Frame")
	frame.Size = UDim2.new(1, -20, 0, 42)
	frame.Position = UDim2.new(0, 10, 0, yPos)
	frame.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
	frame.Parent = mainFrame

	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = frame

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -10, 0, 18)
	label.Position = UDim2.new(0, 5, 0, 2)
	label.BackgroundTransparency = 1
	label.Text = defaultText
	label.TextColor3 = Color3.fromRGB(255, 255, 255)
	label.TextSize = 12
	label.Font = Enum.Font.SourceSansBold
	label.Parent = frame

	local bar = Instance.new("Frame")
	bar.Size = UDim2.new(1, -20, 0, 8)
	bar.Position = UDim2.new(0, 10, 0, 24)
	bar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	bar.Parent = frame

	local fill = Instance.new("Frame")
	fill.Size = UDim2.new(0, 0, 1, 0)
	fill.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
	fill.BorderSizePixel = 0
	fill.Parent = bar

	local dragging = false

	local function update(input)
		local barPos = bar.AbsolutePosition.X
		local barSize = bar.AbsoluteSize.X
		local mousePos = input.Position.X
		local percentage = math.clamp((mousePos - barPos) / barSize, 0, 1)

		fill.Size = UDim2.new(percentage, 0, 1, 0)
		local val = math.floor((minVal + (percentage * (maxVal - minVal))) * 10) / 10
		label.Text = onUpdate(val)
	end

	bar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = true
			update(input)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			dragging = false
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			update(input)
		end
	end)
end

---------------------------------------------------------
-- WALK & FLY SPEED SLIDERS
---------------------------------------------------------
local function applySpeedMultiplier()
	if localPlayer.Character then
		local humanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")
		if humanoid then
			local baseSpeed = humanoid:GetAttribute("BaseWalkSpeed") or 16
			humanoid.WalkSpeed = baseSpeed * speedMultiplier
		end
	end
end

createSlider(180, "Walk Speed: 1.0x", 1, 10, function(val)
	speedMultiplier = val
	applySpeedMultiplier()
	return string.format("Walk Speed: %.1fx", val)
end)

createSlider(228, "Fly Speed: 1.0x", 1, 10, function(val)
	flySpeedMultiplier = val
	return string.format("Fly Speed: %.1fx", val)
end)

---------------------------------------------------------
-- CUSTOM 99 NIGHTS SUB-MENU & LOGIC
---------------------------------------------------------
local nightsFrame = Instance.new("Frame")
nightsFrame.Name = "NightsFrame"
nightsFrame.Size = UDim2.new(0, 260, 0, 360)
nightsFrame.Position = UDim2.new(0, 540, 0.5, -180)
nightsFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
nightsFrame.BorderSizePixel = 0
nightsFrame.Active = true
nightsFrame.Draggable = true
nightsFrame.Visible = false
nightsFrame.Parent = screenGui

local nightsCorner = Instance.new("UICorner")
nightsCorner.CornerRadius = UDim.new(0, 8)
nightsCorner.Parent = nightsFrame

local nightsTitle = Instance.new("TextLabel")
nightsTitle.Size = UDim2.new(1, -30, 0, 36)
nightsTitle.Position = UDim2.new(0, 10, 0, 0)
nightsTitle.BackgroundTransparency = 1
nightsTitle.Text = "99 Nights Menu"
nightsTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
nightsTitle.TextSize = 14
nightsTitle.TextXAlignment = Enum.TextXAlignment.Left
nightsTitle.Font = Enum.Font.SourceSansBold
nightsTitle.Parent = nightsFrame

local nightsCloseBtn = Instance.new("TextButton")
nightsCloseBtn.Size = UDim2.new(0, 24, 0, 24)
nightsCloseBtn.Position = UDim2.new(1, -29, 0, 6)
nightsCloseBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
nightsCloseBtn.Text = "X"
nightsCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
nightsCloseBtn.TextSize = 13
nightsCloseBtn.Font = Enum.Font.SourceSansBold
nightsCloseBtn.Parent = nightsFrame

local nightsCloseCorner = Instance.new("UICorner")
nightsCloseCorner.CornerRadius = UDim.new(0, 6)
nightsCloseCorner.Parent = nightsCloseBtn

local nightsScroll = Instance.new("ScrollingFrame")
nightsScroll.Size = UDim2.new(1, -20, 1, -50)
nightsScroll.Position = UDim2.new(0, 10, 0, 40)
nightsScroll.BackgroundTransparency = 1
nightsScroll.ScrollBarThickness = 4
nightsScroll.Parent = nightsFrame

local nightsListLayout = Instance.new("UIListLayout")
nightsListLayout.SortOrder = Enum.SortOrder.LayoutOrder
nightsListLayout.Padding = UDim.new(0, 6)
nightsListLayout.Parent = nightsScroll

nightsListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	nightsScroll.CanvasSize = UDim2.new(0, 0, 0, nightsListLayout.AbsoluteContentSize.Y + 10)
end)

local function updateNightsToggleState(visible)
	nightsFrame.Visible = visible
	if visible then
		nightsBtn.Text = "99 Nights: ON"
		nightsBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		nightsBtn.Text = "99 Nights: OFF"
		nightsBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
end

nightsBtn.MouseButton1Click:Connect(function()
	updateNightsToggleState(not nightsFrame.Visible)
end)

nightsCloseBtn.MouseButton1Click:Connect(function()
	updateNightsToggleState(false)
end)

-- 99 Nights State Variables & Data
local teleportTargets = {
	"Alien", "Alien Chest", "Alien Shelf", "Alpha Wolf", "Alpha Wolf Pelt", "Anvil Base", "Apple", "Bandage", "Bear", "Berry",
	"Bolt", "Broken Fan", "Broken Microwave", "Bunny", "Bunny Foot", "Cake", "Carrot", "Chair Set", "Chest", "Chilli",
	"Coal", "Coin Stack", "Crossbow Cultist", "Cultist", "Cultist Gem", "Deer", "Fuel Canister", "Giant Sack", "Good Axe", "Iron Body",
	"Item Chest", "Item Chest2", "Item Chest3", "Item Chest4", "Item Chest6", "Laser Fence Blueprint", "Laser Sword", "Leather Body", "Log", "Lost Child",
	"Lost Child2", "Lost Child3", "Lost Child4", "Medkit", "Meat? Sandwich", "Morsel", "Old Car Engine", "Old Flashlight", "Old Radio", "Oil Barrel",
	"Raygun", "Revolver", "Revolver Ammo", "Rifle", "Rifle Ammo", "Riot Shield", "Sapling", "Seed Box", "Sheet Metal", "Spear",
	"Steak", "Stronghold Diamond Chest", "Tyre", "UFO Component", "UFO Junk", "Washing Machine", "Wolf", "Wolf Corpse", "Wolf Pelt"
}
local AimbotTargets = {"Alien", "Alpha Wolf", "Wolf", "Crossbow Cultist", "Cultist", "Bunny", "Bear", "Polar Bear"}

local itemESPEnabled = false
local npcESPEnabled = false
local AutoTreeFarmEnabled = false
local nightsAimbotEnabled = false
local nightsFlyEnabled = false

local ignoreDistanceFrom = Vector3.new(0, 0, 0)
local minDistance = 50

local function createSubMenuBtn(text, bgCol, parent)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -8, 0, 30)
	btn.BackgroundColor3 = bgCol
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 12
	btn.Font = Enum.Font.SourceSansBold
	btn.Parent = parent
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 6)
	corner.Parent = btn
	return btn
end

-- Section Label Helper
local function createSectionHeader(text)
	local sectionLabel = Instance.new("TextLabel")
	sectionLabel.Size = UDim2.new(1, -8, 0, 24)
	sectionLabel.BackgroundTransparency = 1
	sectionLabel.Text = "-- " .. text .. " --"
	sectionLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
	sectionLabel.TextSize = 12
	sectionLabel.Font = Enum.Font.SourceSansBold
	sectionLabel.Parent = nightsScroll
end

---------------------------------------------------------
-- ITEM SPAWNER & ITEM BRINGER UTILITIES
---------------------------------------------------------
createSectionHeader("Item Utilities")

-- Helper: Move target item model/part to position
local function bringItemToPosition(targetObj, position)
	if not targetObj then return end
	if targetObj:IsA("Model") then
		targetObj:PivotTo(CFrame.new(position))
	elseif targetObj:IsA("BasePart") then
		targetObj.CFrame = CFrame.new(position)
	end
end

-- Helper: Find valid items in workspace
local function getValidItems()
	local found = {}
	for _, obj in ipairs(workspace:GetDescendants()) do
		if table.find(teleportTargets, obj.Name) then
			local pos = nil
			if obj:IsA("Model") then pcall(function() pos = obj:GetPivot().Position end)
			elseif obj:IsA("BasePart") then pos = obj.Position end
			
			if pos and (pos - ignoreDistanceFrom).Magnitude >= minDistance then
				table.insert(found, obj)
			end
		end
	end
	return found
end

-- Item Bringer Button
local btnItemBringer = createSubMenuBtn("Bring All Nearby Items", Color3.fromRGB(220, 120, 0), nightsScroll)
btnItemBringer.MouseButton1Click:Connect(function()
	local char = localPlayer.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	local spawnPos = hrp.Position + (hrp.CFrame.LookVector * 4) + Vector3.new(0, 2, 0)
	local items = getValidItems()
	for _, item in ipairs(items) do
		bringItemToPosition(item, spawnPos + Vector3.new(math.random(-2, 2), 0, math.random(-2, 2)))
	end
end)

-- Item Spawner (Option 1: Random Multi-Spawn)
local btnRandomSpawner = createSubMenuBtn("Spawn Random Items (Multi)", Color3.fromRGB(140, 60, 220), nightsScroll)
btnRandomSpawner.MouseButton1Click:Connect(function()
	local char = localPlayer.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	local items = getValidItems()
	if #items == 0 then return end

	local maxCount = math.min(#items, 10)
	for i = 1, maxCount do
		local randomIndex = math.random(1, #items)
		local selected = items[randomIndex]
		local spawnPos = hrp.Position + (hrp.CFrame.LookVector * 3) + Vector3.new(math.random(-3, 3), 1, math.random(-3, 3))
		bringItemToPosition(selected, spawnPos)
		table.remove(items, randomIndex)
	end
end)

-- Item Spawner (Option 2: Specific Item Selector)
local selectedSpawnItem = teleportTargets[1]

local selectorFrame = Instance.new("Frame")
selectorFrame.Size = UDim2.new(1, -8, 0, 32)
selectorFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
selectorFrame.Parent = nightsScroll
local selCorner = Instance.new("UICorner")
selCorner.CornerRadius = UDim.new(0, 6)
selCorner.Parent = selectorFrame

local selLabel = Instance.new("TextLabel")
selLabel.Size = UDim2.new(0.65, -5, 1, 0)
selLabel.Position = UDim2.new(0, 8, 0, 0)
selLabel.BackgroundTransparency = 1
selLabel.Text = "Item: " .. selectedSpawnItem
selLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
selLabel.TextSize = 11
selLabel.Font = Enum.Font.SourceSansBold
selLabel.TextXAlignment = Enum.TextXAlignment.Left
selLabel.Parent = selectorFrame

local nextItemBtn = Instance.new("TextButton")
nextItemBtn.Size = UDim2.new(0.3, 0, 0.8, 0)
nextItemBtn.Position = UDim2.new(0.68, 0, 0.1, 0)
nextItemBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 75)
nextItemBtn.Text = "Cycle >"
nextItemBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
nextItemBtn.TextSize = 11
nextItemBtn.Font = Enum.Font.SourceSansBold
nextItemBtn.Parent = selectorFrame
local nCorner = Instance.new("UICorner")
nCorner.CornerRadius = UDim.new(0, 4)
nCorner.Parent = nextItemBtn

local currentItemIndex = 1
nextItemBtn.MouseButton1Click:Connect(function()
	currentItemIndex = (currentItemIndex % #teleportTargets) + 1
	selectedSpawnItem = teleportTargets[currentItemIndex]
	selLabel.Text = "Item: " .. selectedSpawnItem
end)

local btnSpawnSpecific = createSubMenuBtn("Spawn Specific Item", Color3.fromRGB(0, 162, 255), nightsScroll)
btnSpawnSpecific.MouseButton1Click:Connect(function()
	local char = localPlayer.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	for _, obj in ipairs(workspace:GetDescendants()) do
		if obj.Name == selectedSpawnItem then
			local pos = nil
			if obj:IsA("Model") then pcall(function() pos = obj:GetPivot().Position end)
			elseif obj:IsA("BasePart") then pos = obj.Position end
			
			if pos and (pos - ignoreDistanceFrom).Magnitude >= minDistance then
				local spawnPos = hrp.Position + (hrp.CFrame.LookVector * 4) + Vector3.new(0, 1, 0)
				bringItemToPosition(obj, spawnPos)
				break
			end
		end
	end
end)

-- Teleport Shortcuts
createSectionHeader("Shortcuts")
local btnCampfire = createSubMenuBtn("TP to Campfire", Color3.fromRGB(0, 162, 255), nightsScroll)
btnCampfire.MouseButton1Click:Connect(function()
	if localPlayer.Character then localPlayer.Character:PivotTo(CFrame.new(0, 10, 0)) end
end)

local btnGrinder = createSubMenuBtn("TP to Grinder", Color3.fromRGB(0, 162, 255), nightsScroll)
btnGrinder.MouseButton1Click:Connect(function()
	if localPlayer.Character then localPlayer.Character:PivotTo(CFrame.new(16.1, 4, -4.6)) end
end)

-- Item ESP Implementation
local function createESP(item)
	local adorneePart = item:IsA("Model") and item:FindFirstChildWhichIsA("BasePart") or (item:IsA("BasePart") and item)
	if not adorneePart or item:FindFirstChildWhichIsA("Humanoid") then return end

	local distance = (adorneePart.Position - ignoreDistanceFrom).Magnitude
	if distance < minDistance then return end

	if not item:FindFirstChild("ESP_Billboard") then
		local billboard = Instance.new("BillboardGui")
		billboard.Name = "ESP_Billboard"
		billboard.Adornee = adorneePart
		billboard.Size = UDim2.new(0, 50, 0, 20)
		billboard.AlwaysOnTop = true
		billboard.StudsOffset = Vector3.new(0, 2, 0)

		local label = Instance.new("TextLabel", billboard)
		label.Size = UDim2.new(1, 0, 1, 0)
		label.Text = item.Name
		label.BackgroundTransparency = 1
		label.TextColor3 = Color3.fromRGB(255, 255, 255)
		label.TextStrokeTransparency = 0
		label.TextScaled = true
		billboard.Parent = item
	end

	if not item:FindFirstChild("ESP_Highlight") then
		local highlight = Instance.new("Highlight")
		highlight.Name = "ESP_Highlight"
		highlight.FillColor = Color3.fromRGB(255, 85, 0)
		highlight.OutlineColor = Color3.fromRGB(0, 100, 0)
		highlight.FillTransparency = 0.25
		highlight.OutlineTransparency = 0
		highlight.Adornee = item:IsA("Model") and item or adorneePart
		highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
		highlight.Parent = item
	end
end

local function toggleItemESP(state)
	itemESPEnabled = state
	for _, item in pairs(workspace:GetDescendants()) do
		if table.find(teleportTargets, item.Name) then
			if itemESPEnabled then
				createESP(item)
			else
				if item:FindFirstChild("ESP_Billboard") then item.ESP_Billboard:Destroy() end
				if item:FindFirstChild("ESP_Highlight") then item.ESP_Highlight:Destroy() end
			end
		end
	end
end

workspace.DescendantAdded:Connect(function(desc)
	if itemESPEnabled and table.find(teleportTargets, desc.Name) then
		task.wait(0.1)
		createESP(desc)
	end
end)

local btnItemESP = createSubMenuBtn("Item ESP: OFF", Color3.fromRGB(180, 50, 50), nightsScroll)
btnItemESP.MouseButton1Click:Connect(function()
	toggleItemESP(not itemESPEnabled)
	btnItemESP.Text = itemESPEnabled and "Item ESP: ON" or "Item ESP: OFF"
	btnItemESP.BackgroundColor3 = itemESPEnabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(180, 50, 50)
end)

-- NPC ESP Implementation
local npcBoxes = {}

local function createNPCESP(npc)
	if not npc:IsA("Model") or npc:FindFirstChild("HumanoidRootPart") == nil or npcBoxes[npc] then return end

	local box = Drawing.new("Square")
	box.Thickness = 2
	box.Transparency = 1
	box.Color = Color3.fromRGB(255, 85, 0)
	box.Filled = false
	box.Visible = true

	local nameText = Drawing.new("Text")
	nameText.Text = npc.Name
	nameText.Color = Color3.fromRGB(255, 255, 255)
	nameText.Size = 16
	nameText.Center = true
	nameText.Outline = true
	nameText.Visible = true

	npcBoxes[npc] = {box = box, name = nameText}

	npc.AncestryChanged:Connect(function(_, parent)
		if not parent and npcBoxes[npc] then
			npcBoxes[npc].box:Remove()
			npcBoxes[npc].name:Remove()
			npcBoxes[npc] = nil
		end
	end)
end

local function toggleNPCESP(state)
	npcESPEnabled = state
	if not state then
		for npc, visuals in pairs(npcBoxes) do
			if visuals.box then visuals.box:Remove() end
			if visuals.name then visuals.name:Remove() end
		end
		npcBoxes = {}
	else
		for _, obj in ipairs(workspace:GetDescendants()) do
			if table.find(AimbotTargets, obj.Name) and obj:IsA("Model") then
				createNPCESP(obj)
			end
		end
	end
end

workspace.DescendantAdded:Connect(function(desc)
	if table.find(AimbotTargets, desc.Name) and desc:IsA("Model") then
		task.wait(0.1)
		if npcESPEnabled then createNPCESP(desc) end
	end
end)

RunService.RenderStepped:Connect(function()
	for npc, visuals in pairs(npcBoxes) do
		local box = visuals.box
		local name = visuals.name

		if npc and npc:FindFirstChild("HumanoidRootPart") then
			local hrp = npc.HumanoidRootPart
			local size = Vector2.new(60, 80)
			local screenPos, onScreen = camera:WorldToViewportPoint(hrp.Position)

			if onScreen then
				box.Position = Vector2.new(screenPos.X - size.X / 2, screenPos.Y - size.Y / 2)
				box.Size = size
				box.Visible = true

				name.Position = Vector2.new(screenPos.X, screenPos.Y - size.Y / 2 - 15)
				name.Visible = true
			else
				box.Visible = false
				name.Visible = false
			end
		else
			box:Remove()
			name:Remove()
			npcBoxes[npc] = nil
		end
	end
end)

local btnNpcESP = createSubMenuBtn("NPC ESP: OFF", Color3.fromRGB(180, 50, 50), nightsScroll)
btnNpcESP.MouseButton1Click:Connect(function()
	toggleNPCESP(not npcESPEnabled)
	btnNpcESP.Text = npcESPEnabled and "NPC ESP: ON" or "NPC ESP: OFF"
	btnNpcESP.BackgroundColor3 = npcESPEnabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(180, 50, 50)
end)

-- Auto Tree Farm
local btnTreeFarm = createSubMenuBtn("Auto Tree Farm: OFF", Color3.fromRGB(180, 50, 50), nightsScroll)
btnTreeFarm.MouseButton1Click:Connect(function()
	AutoTreeFarmEnabled = not AutoTreeFarmEnabled
	btnTreeFarm.Text = AutoTreeFarmEnabled and "Auto Tree Farm: ON" or "Auto Tree Farm: OFF"
	btnTreeFarm.BackgroundColor3 = AutoTreeFarmEnabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(180, 50, 50)
end)

local badTrees = {}
local function mouse1click()
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
end

task.spawn(function()
	while true do
		if AutoTreeFarmEnabled then
			local trees = {}
			for _, obj in pairs(workspace:GetDescendants()) do
				if obj.Name == "Trunk" and obj.Parent and obj.Parent.Name == "Small Tree" then
					local distance = (obj.Position - ignoreDistanceFrom).Magnitude
					if distance > minDistance and not badTrees[obj:GetFullName()] then
						table.insert(trees, obj)
					end
				end
			end

			table.sort(trees, function(a, b)
				return (a.Position - localPlayer.Character.HumanoidRootPart.Position).Magnitude <
					(b.Position - localPlayer.Character.HumanoidRootPart.Position).Magnitude
			end)

			for _, trunk in ipairs(trees) do
				if not AutoTreeFarmEnabled then break end
				if localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") then
					localPlayer.Character:PivotTo(trunk.CFrame + Vector3.new(0, 3, 0))
					task.wait(0.2)
					local startTime = tick()
					while AutoTreeFarmEnabled and trunk and trunk.Parent and trunk.Parent.Name == "Small Tree" do
						mouse1click()
						task.wait(0.2)
						if tick() - startTime > 12 then
							badTrees[trunk:GetFullName()] = true
							break
						end
					end
					task.wait(0.3)
				end
			end
		end
		task.wait(1.5)
	end
end)

-- 99 Nights Aimbot
local FOVRadius = 100
local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(128, 255, 0)
FOVCircle.Thickness = 1
FOVCircle.Radius = FOVRadius
FOVCircle.Transparency = 0.5
FOVCircle.Filled = false
FOVCircle.Visible = false

local btnNightsAimbot = createSubMenuBtn("Aimbot (Right Click): OFF", Color3.fromRGB(180, 50, 50), nightsScroll)
btnNightsAimbot.MouseButton1Click:Connect(function()
	nightsAimbotEnabled = not nightsAimbotEnabled
	btnNightsAimbot.Text = nightsAimbotEnabled and "Aimbot (Right Click): ON" or "Aimbot (Right Click): OFF"
	btnNightsAimbot.BackgroundColor3 = nightsAimbotEnabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(180, 50, 50)
end)

local lastAimbotCheck = 0
local aimbotCheckInterval = 0.02
local smoothness = 0.2

RunService.RenderStepped:Connect(function()
	if not nightsAimbotEnabled or not UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
		FOVCircle.Visible = false
		return
	end

	local currentTime = tick()
	if currentTime - lastAimbotCheck < aimbotCheckInterval then return end
	lastAimbotCheck = currentTime

	local mousePos = UserInputService:GetMouseLocation()
	local closestTarget, shortestDistance = nil, math.huge

	for _, obj in ipairs(workspace:GetDescendants()) do
		if table.find(AimbotTargets, obj.Name) and obj:IsA("Model") then
			local head = obj:FindFirstChild("Head")
			if head then
				local screenPos, onScreen = camera:WorldToViewportPoint(head.Position)
				if onScreen then
					local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
					if dist < shortestDistance and dist <= FOVRadius then
						shortestDistance = dist
						closestTarget = head
					end
				end
			end
		end
	end

	if closestTarget then
		local currentCF = camera.CFrame
		local targetCF = CFrame.new(camera.CFrame.Position, closestTarget.Position)
		camera.CFrame = currentCF:Lerp(targetCF, smoothness)
		FOVCircle.Position = Vector2.new(mousePos.X, mousePos.Y)
		FOVCircle.Visible = true
	else
		FOVCircle.Visible = false
	end
end)

-- 99 Nights Fly Feature
local nightsFlyConn = nil
local btnNightsFly = createSubMenuBtn("Fly (Q Toggle): OFF", Color3.fromRGB(180, 50, 50), nightsScroll)

local function startNightsFly()
	local hrp = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return end

	local bodyGyro = Instance.new("BodyGyro", hrp)
	local bodyVelocity = Instance.new("BodyVelocity", hrp)
	bodyGyro.P = 9e4
	bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
	bodyGyro.CFrame = hrp.CFrame
	bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)

	nightsFlyConn = RunService.RenderStepped:Connect(function()
		local moveVec = Vector3.zero
		local camCF = camera.CFrame
		if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVec += camCF.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVec -= camCF.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVec -= camCF.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVec += camCF.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveVec += camCF.UpVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveVec -= camCF.UpVector end
		bodyVelocity.Velocity = moveVec.Magnitude > 0 and moveVec.Unit * 60 or Vector3.zero
		bodyGyro.CFrame = camCF
	end)
end

local function stopNightsFly()
	if nightsFlyConn then nightsFlyConn:Disconnect() end
	local hrp = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
	if hrp then
		for _, v in pairs(hrp:GetChildren()) do
			if v:IsA("BodyGyro") or v:IsA("BodyVelocity") then v:Destroy() end
		end
	end
end

local function toggleNightsFly(state)
	nightsFlyEnabled = state
	btnNightsFly.Text = nightsFlyEnabled and "Fly (Q Toggle): ON" or "Fly (Q Toggle): OFF"
	btnNightsFly.BackgroundColor3 = nightsFlyEnabled and Color3.fromRGB(40, 160, 60) or Color3.fromRGB(180, 50, 50)
	if nightsFlyEnabled then startNightsFly() else stopNightsFly() end
end

btnNightsFly.MouseButton1Click:Connect(function()
	toggleNightsFly(not nightsFlyEnabled)
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end
	if input.KeyCode == Enum.KeyCode.Q and nightsFrame.Visible then
		toggleNightsFly(not nightsFlyEnabled)
	end
end)

-- 99 Nights Item Teleports
createSectionHeader("Item Teleports")

for _, itemName in ipairs(teleportTargets) do
	local itemBtn = createSubMenuBtn("TP to " .. itemName, Color3.fromRGB(60, 60, 75), nightsScroll)
	itemBtn.MouseButton1Click:Connect(function()
		local closest, shortest = nil, math.huge
		for _, obj in pairs(workspace:GetDescendants()) do
			if obj.Name == itemName and obj:IsA("Model") then
				local cf = nil
				if pcall(function() cf = obj:GetPivot() end) then
					-- success
				else
					local part = obj:FindFirstChildWhichIsA("BasePart")
					if part then cf = part.CFrame end
				end
				if cf then
					local dist = (cf.Position - ignoreDistanceFrom).Magnitude
					if dist >= minDistance and dist < shortest then
						closest = obj
						shortest = dist
					end
				end
			end
		end
		if closest and localPlayer.Character then
			local cf = nil
			if pcall(function() cf = closest:GetPivot() end) then
				-- success
			else
				local part = closest:FindFirstChildWhichIsA("BasePart")
				if part then cf = part.CFrame end
			end
			if cf then
				localPlayer.Character:PivotTo(cf + Vector3.new(0, 5, 0))
			end
		end
	end)
end

---------------------------------------------------------
-- SEPARATE TELEPORT MENU UI
---------------------------------------------------------
local tpFrame = Instance.new("Frame")
tpFrame.Name = "TeleportFrame"
tpFrame.Size = UDim2.new(0, 240, 0, 270)
tpFrame.Position = UDim2.new(0, 290, 0.5, -135)
tpFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
tpFrame.BorderSizePixel = 0
tpFrame.Active = true
tpFrame.Draggable = true
tpFrame.Visible = false
tpFrame.Parent = screenGui

local tpCorner = Instance.new("UICorner")
tpCorner.CornerRadius = UDim.new(0, 8)
tpCorner.Parent = tpFrame

local tpTitleLabel = Instance.new("TextLabel")
tpTitleLabel.Size = UDim2.new(1, -30, 0, 36)
tpTitleLabel.Position = UDim2.new(0, 10, 0, 0)
tpTitleLabel.BackgroundTransparency = 1
tpTitleLabel.Text = "Teleport Menu"
tpTitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
tpTitleLabel.TextSize = 14
tpTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
tpTitleLabel.Font = Enum.Font.SourceSansBold
tpTitleLabel.Parent = tpFrame

local tpCloseBtn = Instance.new("TextButton")
tpCloseBtn.Size = UDim2.new(0, 24, 0, 24)
tpCloseBtn.Position = UDim2.new(1, -29, 0, 6)
tpCloseBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
tpCloseBtn.Text = "X"
tpCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
tpCloseBtn.TextSize = 13
tpCloseBtn.Font = Enum.Font.SourceSansBold
tpCloseBtn.Parent = tpFrame

local tpCloseCorner = Instance.new("UICorner")
tpCloseCorner.CornerRadius = UDim.new(0, 6)
tpCloseCorner.Parent = tpCloseBtn

local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Size = UDim2.new(1, -20, 0, 140)
scrollFrame.Position = UDim2.new(0, 10, 0, 40)
scrollFrame.BackgroundTransparency = 1
scrollFrame.ScrollBarThickness = 4
scrollFrame.Parent = tpFrame

local uiListLayout = Instance.new("UIListLayout")
uiListLayout.SortOrder = Enum.SortOrder.LayoutOrder
uiListLayout.Padding = UDim.new(0, 4)
uiListLayout.Parent = scrollFrame

local randomTpBtn = createButton("Teleport to Random Player", UDim2.new(0, 10, 0, 190), Color3.fromRGB(140, 60, 220), tpFrame)
local tpButton    = createButton("Teleport to Selected Player", UDim2.new(0, 10, 0, 228), Color3.fromRGB(0, 162, 255), tpFrame)

local function updateTpMenuToggleState(visible)
	tpFrame.Visible = visible
	if visible then
		toggleTpMenuBtn.Text = "TP Menu: ON"
		toggleTpMenuBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		toggleTpMenuBtn.Text = "TP Menu: OFF"
		toggleTpMenuBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
end

toggleTpMenuBtn.MouseButton1Click:Connect(function()
	updateTpMenuToggleState(not tpFrame.Visible)
end)

tpCloseBtn.MouseButton1Click:Connect(function()
	updateTpMenuToggleState(false)
end)

---------------------------------------------------------
-- MAIN AIMBOT SYSTEM
---------------------------------------------------------
local function getNearestHead()
	local localCharacter = localPlayer.Character
	if not localCharacter or not localCharacter:FindFirstChild("HumanoidRootPart") then return nil end

	local localPosition = localCharacter.HumanoidRootPart.Position
	local nearestHead = nil
	local shortestDistance = math.huge

	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= localPlayer and player.Character then
			local targetHead = player.Character:FindFirstChild("Head")
			local targetHumanoid = player.Character:FindFirstChildOfClass("Humanoid")
			
			if targetHead and targetHumanoid and targetHumanoid.Health > 0 then
				local distance = (targetHead.Position - localPosition).Magnitude
				if distance < shortestDistance then
					shortestDistance = distance
					nearestHead = targetHead
				end
			end
		end
	end
	return nearestHead
end

RunService.RenderStepped:Connect(function()
	if not aimbotEnabled then return end

	local character = localPlayer.Character
	if not character then return end
	
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid or humanoid.Health <= 0 then return end

	local targetHead = getNearestHead()
	if targetHead then
		local currentCamPos = camera.CFrame.Position
		camera.CameraType = Enum.CameraType.Scriptable
		camera.CFrame = CFrame.new(currentCamPos, targetHead.Position)
	else
		if camera.CameraType ~= Enum.CameraType.Custom then
			camera.CameraType = Enum.CameraType.Custom
		end
	end
end)

aimbotToggleBtn.MouseButton1Click:Connect(function()
	aimbotEnabled = not aimbotEnabled
	if aimbotEnabled then
		aimbotToggleBtn.Text = "Aimbot: ON"
		aimbotToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		aimbotToggleBtn.Text = "Aimbot: OFF"
		aimbotToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
		camera.CameraType = Enum.CameraType.Custom
	end
end)

---------------------------------------------------------
-- MAIN ESP HIGHLIGHT SYSTEM
---------------------------------------------------------
local function applyHighlight(character)
	if character == localPlayer.Character then return end
	
	local existingHighlight = character:FindFirstChild("PlayerHighlight")
	if espEnabled then
		if not existingHighlight then
			local highlight = Instance.new("Highlight")
			highlight.Name = "PlayerHighlight"
			highlight.FillColor = HIGHLIGHT_COLOR
			highlight.OutlineColor = OUTLINE_COLOR
			highlight.FillTransparency = FILL_TRANSPARENCY
			highlight.OutlineTransparency = OUTLINE_TRANSPARENCY
			highlight.Adornee = character
			highlight.Parent = character
		end
	else
		if existingHighlight then
			existingHighlight:Destroy()
		end
	end
end

local function updateAllHighlights()
	for _, player in ipairs(Players:GetPlayers()) do
		if player.Character then
			applyHighlight(player.Character)
		end
	end
end

local function onPlayerAdded(player)
	if player == localPlayer then return end
	if player.Character then applyHighlight(player.Character) end
	player.CharacterAdded:Connect(applyHighlight)
end

for _, player in ipairs(Players:GetPlayers()) do onPlayerAdded(player) end
Players.PlayerAdded:Connect(onPlayerAdded)

espToggleBtn.MouseButton1Click:Connect(function()
	espEnabled = not espEnabled
	if espEnabled then
		espToggleBtn.Text = "ESP Highlights: ON"
		espToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		espToggleBtn.Text = "ESP Highlights: OFF"
		espToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
	updateAllHighlights()
end)

---------------------------------------------------------
-- MAIN FLY SYSTEM
---------------------------------------------------------
local bodyVelocity = nil
local bodyGyro = nil

flyToggleBtn.MouseButton1Click:Connect(function()
	flyEnabled = not flyEnabled
	local character = localPlayer.Character
	local root = character and character:FindFirstChild("HumanoidRootPart")

	if flyEnabled then
		flyToggleBtn.Text = "Fly: ON"
		flyToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
		if root then
			bodyVelocity = Instance.new("BodyVelocity")
			bodyVelocity.Velocity = Vector3.zero
			bodyVelocity.MaxForce = Vector3.new(4e5, 4e5, 4e5)
			bodyVelocity.Parent = root

			bodyGyro = Instance.new("BodyGyro")
			bodyGyro.MaxTorque = Vector3.new(4e5, 4e5, 4e5)
			bodyGyro.CFrame = root.CFrame
			bodyGyro.Parent = root
		end
	else
		flyToggleBtn.Text = "Fly: OFF"
		flyToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
		if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
		if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
	end
end)

RunService.RenderStepped:Connect(function()
	if flyEnabled and bodyVelocity and bodyGyro then
		local moveDir = Vector3.zero
		if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camera.CFrame.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camera.CFrame.LookVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camera.CFrame.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camera.CFrame.RightVector end
		if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
		if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end

		local currentFlySpeed = BASE_FLY_SPEED * flySpeedMultiplier
		bodyVelocity.Velocity = moveDir.Magnitude > 0 and moveDir.Unit * currentFlySpeed or Vector3.zero
		bodyGyro.CFrame = camera.CFrame
	end
end)

---------------------------------------------------------
-- MAIN NOCLIP SYSTEM
---------------------------------------------------------
noclipToggleBtn.MouseButton1Click:Connect(function()
	noclipEnabled = not noclipEnabled
	if noclipEnabled then
		noclipToggleBtn.Text = "Noclip: ON"
		noclipToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 160, 60)
	else
		noclipToggleBtn.Text = "Noclip: OFF"
		noclipToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
	end
end)

RunService.Stepped:Connect(function()
	if noclipEnabled and localPlayer.Character then
		for _, part in ipairs(localPlayer.Character:GetDescendants()) do
			if part:IsA("BasePart") then
				part.CanCollide = false
			end
		end
	end
end)

---------------------------------------------------------
-- MAIN TELEPORT SYSTEM
---------------------------------------------------------
local selectedPlayer = nil

uiListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, uiListLayout.AbsoluteContentSize.Y)
end)

local function refreshPlayerList()
	for _, child in ipairs(scrollFrame:GetChildren()) do
		if child:IsA("TextButton") then child:Destroy() end
	end

	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= localPlayer then
			local pButton = Instance.new("TextButton")
			pButton.Size = UDim2.new(1, -6, 0, 28)
			pButton.BackgroundColor3 = (selectedPlayer == player) and Color3.fromRGB(0, 120, 215) or Color3.fromRGB(50, 50, 60)
			pButton.Text = player.DisplayName .. " (@" .. player.Name .. ")"
			pButton.TextColor3 = Color3.fromRGB(200, 200, 200)
			pButton.TextSize = 13
			pButton.Font = Enum.Font.SourceSans
			pButton.Parent = scrollFrame

			local pCorner = Instance.new("UICorner")
			pCorner.CornerRadius = UDim.new(0, 4)
			pCorner.Parent = pButton

			pButton.MouseButton1Click:Connect(function()
				selectedPlayer = player
				for _, btn in ipairs(scrollFrame:GetChildren()) do
					if btn:IsA("TextButton") then
						btn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
					end
				end
				pButton.BackgroundColor3 = Color3.fromRGB(0, 120, 215)
			end)
		end
	end
end

-- Teleport to Selected
tpButton.MouseButton1Click:Connect(function()
	if not selectedPlayer then return end

	local myChar = localPlayer.Character
	local targetChar = selectedPlayer.Character

	if myChar and targetChar then
		local myRoot = myChar:FindFirstChild("HumanoidRootPart")
		local targetRoot = targetChar:FindFirstChild("HumanoidRootPart")

		if myRoot and targetRoot then
			myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 3)
		end
	end
end)

-- Teleport to Random Player
randomTpBtn.MouseButton1Click:Connect(function()
	local validTargets = {}
	
	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= localPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			table.insert(validTargets, player)
		end
	end

	if #validTargets > 0 then
		local randomPlayer = validTargets[math.random(1, #validTargets)]
		local myChar = localPlayer.Character
		local targetChar = randomPlayer.Character

		if myChar and targetChar then
			local myRoot = myChar:FindFirstChild("HumanoidRootPart")
			local targetRoot = targetChar:FindFirstChild("HumanoidRootPart")

			if myRoot and targetRoot then
				myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 3)
			end
		end
	end
end)

---------------------------------------------------------
-- UI TOGGLE & CHARACTER HANDLERS
---------------------------------------------------------
closeButton.MouseButton1Click:Connect(function()
	mainFrame.Visible = false
	updateTpMenuToggleState(false)
	updateNightsToggleState(false)
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if not gameProcessed and input.KeyCode == Enum.KeyCode.RightShift then
		local targetState = not mainFrame.Visible
		mainFrame.Visible = targetState
		if not targetState then
			updateTpMenuToggleState(false)
			updateNightsToggleState(false)
		end
	end
end)

local function onCharacterAdded(char)
	local humanoid = char:WaitForChild("Humanoid")
	if not humanoid:GetAttribute("BaseWalkSpeed") then
		humanoid:SetAttribute("BaseWalkSpeed", humanoid.WalkSpeed)
	end
	applySpeedMultiplier()
end

if localPlayer.Character then
	onCharacterAdded(localPlayer.Character)
end

localPlayer.CharacterAdded:Connect(onCharacterAdded)

Players.PlayerAdded:Connect(refreshPlayerList)
Players.PlayerRemoving:Connect(function(player)
	if selectedPlayer == player then selectedPlayer = nil end
	refreshPlayerList()
end)

refreshPlayerList()

---------------------------------------------------------
-- LOADING ANIMATION SEQUENCE
---------------------------------------------------------
task.spawn(function()
	-- Animate Loading Progress Bar
	local tweenInfo = TweenInfo.new(1.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	local progressTween = TweenService:Create(barFill, tweenInfo, {Size = UDim2.new(1, 0, 1, 0)})
	progressTween:Play()

	task.wait(0.6)
	loadingStatus.Text = "Loading modules..."
	task.wait(0.6)
	loadingStatus.Text = "Ready!"
	task.wait(0.6)

	-- Fade Out Loading UI
	local fadeInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	local fadeOut = TweenService:Create(loadingFrame, fadeInfo, {BackgroundTransparency = 1})
	TweenService:Create(loadingTitle, fadeInfo, {TextTransparency = 1}):Play()
	TweenService:Create(loadingStatus, fadeInfo, {TextTransparency = 1}):Play()
	TweenService:Create(barBackground, fadeInfo, {BackgroundTransparency = 1}):Play()
	TweenService:Create(barFill, fadeInfo, {BackgroundTransparency = 1}):Play()
	fadeOut:Play()
	
	fadeOut.Completed:Connect(function()
		loadingFrame:Destroy()
		mainFrame.Visible = true
	end)
end)
