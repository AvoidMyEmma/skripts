-- LocalScript placed inside StarterPlayerScripts

local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local TweenService = game:GetService("TweenService")

local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

---------------------------------------------------------
-- CONFIGURATION SETTINGS
---------------------------------------------------------
local ATTACK_DURATION = 1.0  -- Time (in seconds) spent tracking & clicking on each player
local CLICK_INTERVAL = 0.03  -- Delay between clicks while attached

local MOVE_SPEED = 50
local LOOK_SENSITIVITY = 0.25
local TOGGLE_KEY = Enum.KeyCode.LeftAlt
local ACTION_FREEZE = "FreecamFreezeMovement"

-- Role Colors (Vibrant Cyber/Modern Palette)
local COLOR_INNOCENT = Color3.fromRGB(46, 213, 115)  -- Neon Mint Green
local COLOR_SHERIFF  = Color3.fromRGB(52, 152, 219)  -- Electric Blue
local COLOR_MURDER   = Color3.fromRGB(255, 71, 87)   -- Crimson Red

---------------------------------------------------------
-- INTERNAL STATE
---------------------------------------------------------
local isTeleportRunning = false
local isAntiMurderActive = false

local isFreecamActive = false
local isSpectating = false
local cameraPos = Vector3.zero
local pitch = 0
local yaw = 0

local spectateIndex = 1
local targetPlayer = nil

local renderConnection = nil
local ghostCharacter = nil
local isRightMouseDown = false

---------------------------------------------------------
-- MAIN GUI CREATION
---------------------------------------------------------
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Clean up pre-existing instances
local existingGui = playerGui:FindFirstChild("MergedControlGui")
if existingGui then existingGui:Destroy() end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MergedControlGui"
screenGui.ResetOnSpawn = false
screenGui.Enabled = true
screenGui.DisplayOrder = 100
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

-- Ultra-Sleek Modern Glassmorphism Helper (Blur-ready look, crisp borders, subtle vignette gradient)
local function applyCyberStyle(parentInstance, cornerRadius, transparency)
    parentInstance.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    parentInstance.BackgroundTransparency = transparency or 0.22
    parentInstance.BorderSizePixel = 0

    local corner = Instance.new("UICorner")
    corner.CornerRadius = cornerRadius or UDim.new(0, 14)
    corner.Parent = parentInstance

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Transparency = 0.8
    stroke.Thickness = 1.2
    stroke.Parent = parentInstance

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(140, 140, 155))
    })
    gradient.Rotation = 90
    gradient.Parent = parentInstance
    
    return stroke
end

-- Smooth Hover & Click Animation helper
local function applyInteractiveButton(button, defaultColor, activeColor, isActiveCheck)
    button.BackgroundColor3 = defaultColor
    button.BackgroundTransparency = 0.2

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = button

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Transparency = 0.75
    stroke.Parent = button

    button.MouseEnter:Connect(function()
        local base = isActiveCheck() and activeColor or defaultColor
        local hoverColor = Color3.fromRGB(
            math.clamp(base.R * 255 + 30, 0, 255),
            math.clamp(base.G * 255 + 30, 0, 255),
            math.clamp(base.B * 255 + 30, 0, 255)
        )
        TweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = hoverColor,
            BackgroundTransparency = 0.1
        }):Play()
    end)
    
    button.MouseLeave:Connect(function()
        local targetColor = isActiveCheck() and activeColor or defaultColor
        TweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundColor3 = targetColor,
            BackgroundTransparency = 0.2
        }):Play()
    end)
end

-- 1. Main Container Frame (Holds TP, Anti-Murder, and Freecam Controls)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 240, 0, 185)
mainFrame.Position = UDim2.new(0.02, 0, 0.3, 0)
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.ZIndex = 10
mainFrame.Parent = screenGui

applyCyberStyle(mainFrame, UDim.new(0, 14), 0.24)

-- Title Bar with glowing indicator status dot
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, -35, 0, 38)
titleLabel.Position = UDim2.new(0, 16, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "  Nexus Hub"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 15
titleLabel.ZIndex = 11
titleLabel.Parent = mainFrame

local titleDot = Instance.new("Frame")
titleDot.Size = UDim2.new(0, 7, 0, 7)
titleDot.Position = UDim2.new(0, 4, 0, 15)
titleDot.BackgroundColor3 = Color3.fromRGB(46, 213, 115)
titleDot.BorderSizePixel = 0
titleDot.ZIndex = 12
titleDot.Parent = titleLabel
local dotCorner = Instance.new("UICorner")
dotCorner.CornerRadius = UDim.new(1, 0)
dotCorner.Parent = titleDot

-- Global Close ("✕") Button
local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -34, 0, 4)
closeButton.BackgroundTransparency = 1
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(180, 180, 195)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 14
closeButton.ZIndex = 11
closeButton.Parent = mainFrame

closeButton.MouseEnter:Connect(function()
    TweenService:Create(closeButton, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(255, 80, 80)}):Play()
end)
closeButton.MouseLeave:Connect(function()
    TweenService:Create(closeButton, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(180, 180, 195)}):Play()
end)

-- TP Loop Start Button
local startButton = Instance.new("TextButton")
startButton.Name = "StartButton"
startButton.Size = UDim2.new(0.9, 0, 0, 36)
startButton.Position = UDim2.new(0.05, 0, 0.24, 0)
startButton.Text = "Start TP Loop"
startButton.TextColor3 = Color3.fromRGB(255, 255, 255)
startButton.Font = Enum.Font.GothamBold
startButton.TextSize = 13
startButton.ZIndex = 11
startButton.Parent = mainFrame
applyInteractiveButton(startButton, Color3.fromRGB(46, 204, 113), Color3.fromRGB(46, 204, 113), function() return isTeleportRunning end)

-- Anti-Murder Toggle Button
local antiMurderBtn = Instance.new("TextButton")
antiMurderBtn.Name = "AntiMurderButton"
antiMurderBtn.Size = UDim2.new(0.9, 0, 0, 36)
antiMurderBtn.Position = UDim2.new(0.05, 0, 0.50, 0)
antiMurderBtn.Text = "Anti-Murder: OFF"
antiMurderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
antiMurderBtn.Font = Enum.Font.GothamBold
antiMurderBtn.TextSize = 13
antiMurderBtn.ZIndex = 11
antiMurderBtn.Parent = mainFrame
applyInteractiveButton(antiMurderBtn, Color3.fromRGB(220, 50, 50), Color3.fromRGB(46, 204, 113), function() return isAntiMurderActive end)

-- Freecam Toggle Button
local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0.9, 0, 0, 36)
toggleButton.Position = UDim2.new(0.05, 0, 0.76, 0)
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextSize = 13
toggleButton.ZIndex = 11
toggleButton.Parent = mainFrame
applyInteractiveButton(toggleButton, Color3.fromRGB(55, 55, 70), Color3.fromRGB(46, 204, 113), function() return isFreecamActive end)

-- Spectate Bar (Bottom Center)
local specFrame = Instance.new("Frame")
specFrame.Size = UDim2.new(0, 330, 0, 50)
specFrame.Position = UDim2.new(0.5, -165, 0.86, 0)
specFrame.Visible = false
specFrame.ZIndex = 10
specFrame.Parent = screenGui
applyCyberStyle(specFrame, UDim.new(0, 12), 0.22)

local prevBtn = Instance.new("TextButton")
prevBtn.Size = UDim2.new(0, 38, 1, -12)
prevBtn.Position = UDim2.new(0, 6, 0, 6)
prevBtn.Text = "◀"
prevBtn.Font = Enum.Font.GothamBold
prevBtn.TextSize = 14
prevBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
prevBtn.ZIndex = 11
prevBtn.Parent = specFrame
applyInteractiveButton(prevBtn, Color3.fromRGB(50, 50, 65), Color3.fromRGB(70, 70, 90), function() return false end)

local nextBtn = Instance.new("TextButton")
nextBtn.Size = UDim2.new(0, 38, 1, -12)
nextBtn.Position = UDim2.new(1, -44, 0, 6)
nextBtn.Text = "▶"
nextBtn.Font = Enum.Font.GothamBold
nextBtn.TextSize = 14
nextBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
nextBtn.ZIndex = 11
nextBtn.Parent = specFrame
applyInteractiveButton(nextBtn, Color3.fromRGB(50, 50, 65), Color3.fromRGB(70, 70, 90), function() return false end)

local nameLabel = Instance.new("TextLabel")
nameLabel.Size = UDim2.new(1, -100, 1, -10)
nameLabel.Position = UDim2.new(0, 50, 0, 5)
nameLabel.BackgroundTransparency = 1
nameLabel.Font = Enum.Font.GothamMedium
nameLabel.TextSize = 13
nameLabel.TextColor3 = Color3.fromRGB(240, 240, 245)
nameLabel.Text = "Spectating: None"
nameLabel.ZIndex = 11
nameLabel.Parent = specFrame

---------------------------------------------------------
-- PLAYER LIST HEADER FRAME (ROLE ESP LIST)
---------------------------------------------------------
local listFrame = Instance.new("Frame")
listFrame.Name = "PlayerListFrame"
listFrame.Size = UDim2.new(0, 240, 0, 270)
listFrame.Position = UDim2.new(0.02, 0, 0.3, 195)
listFrame.Active = true
listFrame.Draggable = true
listFrame.ZIndex = 10
listFrame.Parent = screenGui

applyCyberStyle(listFrame, UDim.new(0, 14), 0.24)

local listTitle = Instance.new("TextLabel")
listTitle.Name = "Header"
listTitle.Size = UDim2.new(1, -18, 0, 38)
listTitle.Position = UDim2.new(0, 16, 0, 0)
listTitle.BackgroundTransparency = 1
listTitle.Text = "Live Player Roles"
listTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
listTitle.Font = Enum.Font.GothamBold
listTitle.TextSize = 14
listTitle.TextXAlignment = Enum.TextXAlignment.Left
listTitle.ZIndex = 11
listTitle.Parent = listFrame

local scrollContainer = Instance.new("ScrollingFrame")
scrollContainer.Size = UDim2.new(1, -12, 1, -44)
scrollContainer.Position = UDim2.new(0, 6, 0, 38)
scrollContainer.BackgroundTransparency = 1
scrollContainer.BorderSizePixel = 0
scrollContainer.ScrollBarThickness = 3
scrollContainer.ScrollBarImageColor3 = Color3.fromRGB(150, 150, 170)
scrollContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollContainer.ZIndex = 11
scrollContainer.Parent = listFrame

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 6)
listLayout.SortOrder = Enum.SortOrder.Name
listLayout.Parent = scrollContainer

listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    scrollContainer.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y)
end)

---------------------------------------------------------
-- HELPER FUNCTIONS & ROLE SCANNER / HIGHLIGHT ESP
---------------------------------------------------------
local function getPlayerRole(player)
    if not player then return "Innocent", COLOR_INNOCENT end
    
    local hasGun = false
    local hasKnifeRole = false

    local function scanTools(container)
        if not container then return end
        for _, child in ipairs(container:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "gun" then
                    hasGun = true
                elseif name == "knife" or name == "kinfe" then
                    hasKnifeRole = true
                end
            end
        end
    end

    scanTools(player.Character)
    scanTools(player:FindFirstChild("Backpack"))

    if hasKnifeRole then
        return "Murder", COLOR_MURDER
    elseif hasGun then
        return "Sheriff", COLOR_SHERIFF
    else
        return "Innocent", COLOR_INNOCENT
    end
end

local function applyHighlight(player, roleText, roleColor)
    local char = player.Character
    if not char then return end

    local existingHighlight = char:FindFirstChild("RoleHighlight")

    if roleText == "Sheriff" or roleText == "Murder" then
        if not existingHighlight then
            existingHighlight = Instance.new("Highlight")
            existingHighlight.Name = "RoleHighlight"
            existingHighlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            existingHighlight.FillTransparency = 0.5
            existingHighlight.OutlineTransparency = 0.05
            existingHighlight.Parent = char
        end
        existingHighlight.FillColor = roleColor
        existingHighlight.OutlineColor = roleColor
    else
        if existingHighlight then
            existingHighlight:Destroy()
        end
    end
end

local function updatePlayerList()
    for _, child in ipairs(scrollContainer:GetChildren()) do
        if child:IsA("Frame") then
            child:Destroy()
        end
    end

    for _, p in ipairs(Players:GetPlayers()) do
        local roleText, roleColor = getPlayerRole(p)

        -- Update 3D Character Highlight
        applyHighlight(p, roleText, roleColor)

        -- Update UI List Row
        local rowFrame = Instance.new("Frame")
        rowFrame.Name = p.Name
        rowFrame.Size = UDim2.new(1, -4, 0, 28)
        rowFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 34)
        rowFrame.BackgroundTransparency = 0.35
        rowFrame.BorderSizePixel = 0
        rowFrame.ZIndex = 12

        local rowCorner = Instance.new("UICorner")
        rowCorner.CornerRadius = UDim.new(0, 8)
        rowCorner.Parent = rowFrame
        
        local rowStroke = Instance.new("UIStroke")
        rowStroke.Color = Color3.fromRGB(255, 255, 255)
        rowStroke.Transparency = 0.88
        rowStroke.Parent = rowFrame

        local pNameLabel = Instance.new("TextLabel")
        pNameLabel.Size = UDim2.new(0.6, 0, 1, 0)
        pNameLabel.Position = UDim2.new(0, 10, 0, 0)
        pNameLabel.BackgroundTransparency = 1
        pNameLabel.Text = p.DisplayName
        pNameLabel.TextColor3 = Color3.fromRGB(240, 240, 245)
        pNameLabel.TextXAlignment = Enum.TextXAlignment.Left
        pNameLabel.Font = Enum.Font.GothamMedium
        pNameLabel.TextSize = 12
        pNameLabel.ZIndex = 13
        pNameLabel.Parent = rowFrame

        local roleLabel = Instance.new("TextLabel")
        roleLabel.Size = UDim2.new(0.4, -10, 1, 0)
        roleLabel.Position = UDim2.new(0.6, 0, 0, 0)
        roleLabel.BackgroundTransparency = 1
        roleLabel.Text = roleText
        roleLabel.TextColor3 = roleColor
        roleLabel.TextXAlignment = Enum.TextXAlignment.Right
        roleLabel.Font = Enum.Font.GothamBold
        roleLabel.TextSize = 12
        roleLabel.ZIndex = 13
        roleLabel.Parent = rowFrame

        rowFrame.Parent = scrollContainer
    end
end

-- Refresh player list and highlights regularly in background
task.spawn(function()
    while true do
        updatePlayerList()
        task.wait(1)
    end
end)

local function getEquippedToolType(character)
    if not character then return "None" end
    for _, child in ipairs(character:GetChildren()) do
        if child:IsA("Tool") then
            local name = string.lower(child.Name)
            if name == "gun" then
                return "Gun"
            elseif name == "knife" or name == "kinfe" then
                return "Knife"
            end
        end
    end
    return "None"
end

local function hasKnife(player)
    if player.Character then
        for _, child in ipairs(player.Character:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "knife" or name == "kinfe" then
                    return true
                end
            end
        end
    end

    local backpack = player:FindFirstChild("Backpack")
    if backpack then
        for _, child in ipairs(backpack:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "knife" or name == "kinfe" then
                    return true
                end
            end
        end
    end

    return false
end

local function simulateLeftClick()
    local viewport = workspace.CurrentCamera.ViewportSize
    local centerX = viewport.X / 2
    local centerY = viewport.Y / 2
    
    VirtualInputManager:SendMouseButtonEvent(centerX, centerY, 0, true, game, 0)
    task.wait(0.01)
    VirtualInputManager:SendMouseButtonEvent(centerX, centerY, 0, false, game, 0)
end

-- Continuous background auto-walk task for TP Loop
task.spawn(function()
    while true do
        task.wait(0.5)
        if isTeleportRunning then
            local character = localPlayer.Character
            if character then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if humanoid and hrp then
                    local randomOffset = Vector3.new(
                        math.random(-30, 30),
                        0,
                        math.random(-30, 30)
                    )
                    humanoid:MoveTo(hrp.Position + randomOffset)
                end
            end
        end
    end
end)

-- Helper function to check if a character part is visible on screen (In FOV & Line of Sight)
local function isVisibleToPlayer(targetPart)
    local char = localPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
    
    local myPos = char.HumanoidRootPart.Position
    local targetPos = targetPart.Position
    if (targetPos - myPos).Magnitude > 60 then
        return false
    end
    
    local _, onScreen = camera:WorldToViewportPoint(targetPos)
    if not onScreen then
        return false
    end
    
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {char, targetPart.Parent}
    raycastParams.IgnoreWater = true
    
    local rayResult = workspace:Raycast(camera.CFrame.Position, targetPos - camera.CFrame.Position, raycastParams)
    if rayResult then
        return false
    end
    
    return true
end

-- Anti-Murder logic loop (Triggers when Murder is within 20 studs AND visible)
task.spawn(function()
    while true do
        task.wait(0.1)
        if isAntiMurderActive then
            local myChar = localPlayer.Character
            if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                local myHRP = myChar.HumanoidRootPart
                local myPos = myHRP.Position
                
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= localPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        local role, _ = getPlayerRole(p)
                        if role == "Murder" then
                            local targetPart = p.Character:FindFirstChild("HumanoidRootPart")
                            local dist = (targetPart.Position - myPos).Magnitude
                            
                            if dist <= 20 and isVisibleToPlayer(targetPart) then
                                myHRP.CFrame = myHRP.CFrame * CFrame.new(0, 100, 0)
                                task.wait(2)
                                break
                            end
                        end
                    end
                end
            end
        end
    end
end)

local function startTeleportSequence()
    if isTeleportRunning then return end

    local myCharacter = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local myHRP = myCharacter:WaitForChild("HumanoidRootPart")
    local toolType = getEquippedToolType(myCharacter)

    isTeleportRunning = true
    startButton.Text = "Running..."
    startButton.BackgroundColor3 = Color3.fromRGB(120, 120, 135)

    if toolType == "None" then
        myHRP.CFrame = myHRP.CFrame * CFrame.new(0, 100, 0)
        task.wait(5)
    else
        local visitedPlayers = {}

        for _, targetP in ipairs(Players:GetPlayers()) do
            local currentTool = getEquippedToolType(myCharacter)
            if currentTool == "None" or not isTeleportRunning then
                break
            end

            if targetP ~= localPlayer and not visitedPlayers[targetP] then
                visitedPlayers[targetP] = true

                local shouldTarget = false
                if currentTool == "Gun" then
                    shouldTarget = hasKnife(targetP)
                elseif currentTool == "Knife" then
                    shouldTarget = true
                end

                if shouldTarget then
                    local targetChar = targetP.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        local targetHRP = targetChar.HumanoidRootPart
                        local startTime = os.clock()
                        local lastClick = 0

                        while isTeleportRunning and (os.clock() - startTime < ATTACK_DURATION) do
                            local liveTool = getEquippedToolType(myCharacter)
                            if liveTool == "None" then break end
                            if not targetChar or not targetHRP.Parent then break end

                            myHRP.CFrame = targetHRP.CFrame

                            if os.clock() - lastClick >= CLICK_INTERVAL then
                                simulateLeftClick()
                                lastClick = os.clock()
                            end

                            RunService.Heartbeat:Wait()
                        end
                    end
                end
            end
        end
    end

    isTeleportRunning = false
    startButton.Text = "Start TP Loop"
    startButton.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
end

---------------------------------------------------------
-- FREECAM LOGIC
---------------------------------------------------------
local function updateButtonUI()
    if isFreecamActive then
        toggleButton.Text = "FREECAM: ON (ALT)"
        toggleButton.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
        toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        specFrame.Visible = true
    else
        toggleButton.Text = "FREECAM: OFF (ALT)"
        toggleButton.BackgroundColor3 = Color3.fromRGB(55, 55, 70)
        toggleButton.TextColor3 = Color3.fromRGB(210, 210, 215)
        specFrame.Visible = false
    end
end

local function getSpectateTargets()
    local targets = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= localPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
            table.insert(targets, p)
        end
    end
    return targets
end

local function setSpectateTarget(index)
    local targets = getSpectateTargets()
    if #targets == 0 then
        isSpectating = false
        targetPlayer = nil
        nameLabel.Text = "Freecam Mode (No Players)"
        return
    end

    if index > #targets then index = 1 end
    if index < 1 then index = #targets end

    spectateIndex = index
    targetPlayer = targets[spectateIndex]
    isSpectating = true
    nameLabel.Text = "Spectating: " .. targetPlayer.DisplayName
end

local function createGhostModel(originalChar)
    local hrp = originalChar:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end

    local ghost = Instance.new("Model")
    ghost.Name = "GhostAvatar"

    local ghostHrp = hrp:Clone()
    ghostHrp.Transparency = 0.5
    ghostHrp.Color = Color3.fromRGB(150, 200, 255)
    ghostHrp.Material = Enum.Material.ForceField
    ghostHrp.CanCollide = false
    ghostHrp.Anchored = true
    ghostHrp.Parent = ghost

    ghost.PrimaryPart = ghostHrp
    ghost.Parent = workspace
    return ghost
end

local function sinkInput()
    return Enum.ContextActionResult.Sink
end

local function freezeCharacterMovement()
    local character = localPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid:Move(Vector3.zero, true)
    end
    
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.AssemblyLinearVelocity = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
    end
end

local function restoreCameraToPlayer()
    local character = localPlayer.Character
    if not character then return end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local hrp = character:FindFirstChild("HumanoidRootPart")

    if humanoid then
        camera.CameraType = Enum.CameraType.Track
        camera.CameraSubject = humanoid
        
        if hrp then
            camera.CFrame = hrp.CFrame * CFrame.new(0, 2, 8)
        end

        task.defer(function()
            camera.CameraType = Enum.CameraType.Custom
            camera.CameraSubject = humanoid
        end)
    end
end

local function disableFreecam()
    isFreecamActive = false
    isSpectating = false
    isRightMouseDown = false
    targetPlayer = nil

    if renderConnection then
        renderConnection:Disconnect()
        renderConnection = nil
    end

    ContextActionService:UnbindAction(ACTION_FREEZE)

    if ghostCharacter then
        ghostCharacter:Destroy()
        ghostCharacter = nil
    end

    restoreCameraToPlayer()
    UserInputService.MouseBehavior = Enum.MouseBehavior.Default

    updateButtonUI()
end

local function teleportToFreecam()
    if not isFreecamActive then return end

    local targetCFrame = camera.CFrame
    local character = localPlayer.Character
    if character then
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = targetCFrame
        end
    end

    disableFreecam()
end

local function enableFreecam()
    if isFreecamActive then return end

    local realCharacter = localPlayer.Character
    if not realCharacter or not realCharacter:FindFirstChild("HumanoidRootPart") then return end

    isFreecamActive = true
    isSpectating = false
    targetPlayer = nil
    nameLabel.Text = "Free Flying"

    updateButtonUI()
    freezeCharacterMovement()

    ContextActionService:BindActionAtPriority(
        ACTION_FREEZE,
        sinkInput,
        false,
        Enum.ContextActionPriority.High.Value,
        Enum.KeyCode.W,
        Enum.KeyCode.A,
        Enum.KeyCode.S,
        Enum.KeyCode.D,
        Enum.KeyCode.Space,
        Enum.KeyCode.LeftShift,
        Enum.KeyCode.ButtonA
    )

    ghostCharacter = createGhostModel(realCharacter)

    local rx, ry, _ = camera.CFrame:ToOrientation()
    pitch = rx
    yaw = ry
    cameraPos = camera.CFrame.Position

    camera.CameraType = Enum.CameraType.Scriptable
    UserInputService.MouseBehavior = Enum.MouseBehavior.Default

    renderConnection = RunService.RenderStepped:Connect(function(deltaTime)
        if not isFreecamActive then return end

        freezeCharacterMovement()

        if isRightMouseDown then
            local mouseDelta = UserInputService:GetMouseDelta()
            yaw = yaw - math.rad(mouseDelta.X * LOOK_SENSITIVITY)
            pitch = pitch - math.rad(mouseDelta.Y * LOOK_SENSITIVITY)
            pitch = math.clamp(pitch, math.rad(-89), math.rad(89))
        end

        local cameraRotation = CFrame.Angles(0, yaw, 0) * CFrame.Angles(pitch, 0, 0)

        local moveVector = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVector += Vector3.new(0, 0, -1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVector += Vector3.new(0, 0, 1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVector += Vector3.new(-1, 0, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVector += Vector3.new(1, 0, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.E) or UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveVector += Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.Q) or UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveVector += Vector3.new(0, -1, 0) end

        if moveVector.Magnitude > 0 and isSpectating then
            isSpectating = false
            targetPlayer = nil
            nameLabel.Text = "Free Flying"
        end

        if isSpectating and targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local targetPos = targetPlayer.Character.HumanoidRootPart.Position
            cameraPos = targetPos - (cameraRotation.LookVector * 12) + Vector3.new(0, 3, 0)
        else
            local speed = MOVE_SPEED
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                speed *= 2
            end
            cameraPos += (cameraRotation * moveVector) * speed * deltaTime
        end

        camera.CFrame = CFrame.new(cameraPos) * cameraRotation
    end)
end

---------------------------------------------------------
-- CONNECTIONS & BINDINGS
---------------------------------------------------------
startButton.MouseButton1Click:Connect(startTeleportSequence)

antiMurderBtn.MouseButton1Click:Connect(function()
    isAntiMurderActive = not isAntiMurderActive
    if isAntiMurderActive then
        antiMurderBtn.Text = "Anti-Murder: ON"
        antiMurderBtn.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
    else
        antiMurderBtn.Text = "Anti-Murder: OFF"
        antiMurderBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
    end
end)

prevBtn.MouseButton1Click:Connect(function()
    if isFreecamActive then
        setSpectateTarget(spectateIndex - 1)
    end
end)

nextBtn.MouseButton1Click:Connect(function()
    if isFreecamActive then
        setSpectateTarget(spectateIndex + 1)
    end
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isRightMouseDown = true
        if isFreecamActive then
            UserInputService.MouseBehavior = Enum.MouseBehavior.LockCurrentPosition
        end
    elseif input.UserInputType == Enum.UserInputType.MouseButton3 then
        if isFreecamActive then
            teleportToFreecam()
        end
    elseif input.KeyCode == TOGGLE_KEY and not gameProcessed then
        if isFreecamActive then
            disableFreecam()
        else
            enableFreecam()
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isRightMouseDown = false
        if isFreecamActive then
            UserInputService.MouseBehavior = Enum.MouseBehavior.Default
        end
    end
end)

toggleButton.MouseButton1Click:Connect(function()
    if isFreecamActive then
        disableFreecam()
    else
        enableFreecam()
    end
end)

closeButton.MouseButton1Click:Connect(function()
    isTeleportRunning = false
    isAntiMurderActive = false
    disableFreecam()
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character and p.Character:FindFirstChild("RoleHighlight") then
            p.Character.RoleHighlight:Destroy()
        end
    end

    screenGui:Destroy()
end)

localPlayer.CharacterAdded:Connect(function()
    disableFreecam()
end)

updateButtonUI()
