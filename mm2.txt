-- LocalScript placed inside StarterPlayerScripts or executed via exploit
-- Nexo Hub Pro - Animated Edition with Dynamic Visuals & Fully Configurable Keybinds

local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")
local LogService = game:GetService("LogService")
local TweenService = game:GetService("TweenService")

-- Safely get LocalPlayer and PlayerGui for both studio and exploit environments
local localPlayer = Players.LocalPlayer
if not localPlayer then
    Players:GetPropertyChangedSignal("LocalPlayer"):Wait()
    localPlayer = Players.LocalPlayer
end

local camera = workspace.CurrentCamera

---------------------------------------------------------
-- CONFIGURATION SETTINGS (CONFIGURABLE VIA UI)
---------------------------------------------------------
local DEFAULT_CONFIG = {
    ATTACK_DURATION = 1.0,
    CLICK_INTERVAL = 0.03,
    MOVE_SPEED = 50,
    LOOK_SENSITIVITY = 0.25,
    
    -- Keybinds per module
    KEY_TP_LOOP = Enum.KeyCode.T,
    KEY_ANTI_MURDER = Enum.KeyCode.Y,
    KEY_MURDER_KA = Enum.KeyCode.U,
    KEY_SHERIFF_KA = Enum.KeyCode.I,
    KEY_FREECAM = Enum.KeyCode.LeftAlt,
    
    -- Configurable Distances & Stats
    MURDER_KA_DISTANCE = 10,
    SHERIFF_KA_DISTANCE = 10,
    ANTI_MURDER_DISTANCE = 20,
    ANTI_MURDER_SKY_HEIGHT = 100,
    
    -- GUI Transparency Settings (0 = Opaque, 1 = Fully Transparent)
    GUI_TRANSPARENCY = 0.1,
    ELEMENT_TRANSPARENCY = 0.1,
}

-- Active CONFIG table that updates during runtime
local CONFIG = {}
for k, v in pairs(DEFAULT_CONFIG) do
    CONFIG[k] = v
end

local ACTION_FREEZE = "FreecamFreezeMovement"

-- Role Colors
local COLOR_INNOCENT = Color3.fromRGB(50, 200, 50)  
local COLOR_SHERIFF  = Color3.fromRGB(50, 120, 255) 
local COLOR_MURDER   = Color3.fromRGB(230, 50, 50)  

---------------------------------------------------------
-- INTERNAL STATE
---------------------------------------------------------
local isTeleportRunning = false
local isAntiMurderActive = false
local isMurderKillAuraActive = false
local isSheriffKillAuraActive = false

local isFreecamActive = false
local isSpectating = false
local cameraPos = Vector3.zero
local pitch = 0
local yaw = 0

local spectateIndex = 1
local targetPlayer = nil

local renderConnection = nil
local ghostCharacter = nil
local isRightMouseDown = false
local listeningForKeyConfig = nil

---------------------------------------------------------
-- MAIN GUI CREATION
---------------------------------------------------------
local playerGui = localPlayer:WaitForChild("PlayerGui")

local existingGui = playerGui:FindFirstChild("NexoHubGui")
if existingGui then existingGui:Destroy() end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "NexoHubGui"
screenGui.ResetOnSpawn = false
screenGui.Enabled = true
screenGui.DisplayOrder = 100
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

-- 1. Main Container Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 220, 0, 255)
mainFrame.Position = UDim2.new(0.02, 0, 0.25, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
mainFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.ZIndex = 10
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Name = "AnimatedStroke"
mainStroke.Transparency = 0.4
mainStroke.Thickness = 1.5
mainStroke.Parent = mainFrame

local mainStrokeGradient = Instance.new("UIGradient")
mainStrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 80, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 100, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 200, 255))
})
mainStrokeGradient.Parent = mainStroke

-- Animated Gradient Flow (Smooth shifting without size adjustments)
task.spawn(function()
    local offset = 0
    while true do
        offset = (offset + 0.01) % 1
        mainStrokeGradient.Offset = Vector2.new(offset, 0)
        
        local tween1 = TweenService:Create(mainStroke, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.1, Thickness = 2.2})
        tween1:Play()
        tween1.Completed:Wait()
        local tween2 = TweenService:Create(mainStroke, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.6, Thickness = 1.2})
        tween2:Play()
        tween2.Completed:Wait()
    end
end)

-- Title Bar with Gradient Shift
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, -70, 0, 30)
titleLabel.Position = UDim2.new(0, 12, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "NEXO HUB"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 15
titleLabel.ZIndex = 11
titleLabel.Parent = mainFrame

local titleGradient = Instance.new("UIGradient")
titleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.3, Color3.fromRGB(200, 140, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.7, Color3.fromRGB(140, 200, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})
titleGradient.Parent = titleLabel

task.spawn(function()
    local offset = 0
    while true do
        offset = (offset + 0.012) % 1
        titleGradient.Offset = Vector2.new(offset, 0)
        RunService.RenderStepped:Wait()
    end
end)

-- Settings Button
local settingsButton = Instance.new("TextButton")
settingsButton.Name = "SettingsButton"
settingsButton.Size = UDim2.new(0, 25, 0, 25)
settingsButton.Position = UDim2.new(1, -55, 0, 3)
settingsButton.BackgroundColor3 = Color3.fromRGB(35, 30, 50)
settingsButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
settingsButton.Text = "⚙"
settingsButton.TextColor3 = Color3.fromRGB(200, 170, 255)
settingsButton.Font = Enum.Font.GothamBold
settingsButton.TextSize = 13
settingsButton.ZIndex = 11
settingsButton.Parent = mainFrame

local settingsBtnCorner = Instance.new("UICorner")
settingsBtnCorner.CornerRadius = UDim.new(0, 6)
settingsBtnCorner.Parent = settingsButton

settingsButton.MouseEnter:Connect(function()
    TweenService:Create(settingsButton, TweenInfo.new(0.2), {Rotation = 45, TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
end)
settingsButton.MouseLeave:Connect(function()
    TweenService:Create(settingsButton, TweenInfo.new(0.2), {Rotation = 0, TextColor3 = Color3.fromRGB(200, 170, 255)}):Play()
end)

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 25, 0, 25)
closeButton.Position = UDim2.new(1, -28, 0, 3)
closeButton.BackgroundTransparency = 1
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(255, 90, 110)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 14
closeButton.ZIndex = 11
closeButton.Parent = mainFrame

local function createModuleButton(name, posY, defaultText)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(0.9, 0, 0, 30)
    btn.Position = UDim2.new(0.05, 0, 0, posY)
    btn.BackgroundColor3 = Color3.fromRGB(45, 25, 35)
    btn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    btn.Text = defaultText
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamSemibold
    btn.TextSize = 13
    btn.ZIndex = 11
    btn.Parent = mainFrame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    local btnStroke = Instance.new("UIStroke")
    btnStroke.Color = Color3.fromRGB(255, 255, 255)
    btnStroke.Transparency = 0.85
    btnStroke.Parent = btn

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY - 0.05}):Play()
        TweenService:Create(btnStroke, TweenInfo.new(0.2), {Transparency = 0.3, Color = Color3.fromRGB(160, 120, 255)}):Play()
    end)

    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY}):Play()
        TweenService:Create(btnStroke, TweenInfo.new(0.2), {Transparency = 0.85, Color = Color3.fromRGB(255, 255, 255)}):Play()
    end)

    return btn
end

-- Module Buttons Stack
local startButton = createModuleButton("StartButton", 35, "Start TP Loop (T)")
startButton.BackgroundColor3 = Color3.fromRGB(20, 45, 35)

local antiMurderBtn = createModuleButton("AntiMurderButton", 72, "Anti-Murder: OFF (Y)")
local murderKAButton = createModuleButton("MurderKAButton", 109, "Murder KA: OFF (U)")
local sheriffKAButton = createModuleButton("SheriffKAButton", 146, "Sheriff KA: OFF (I)")
local toggleButton   = createModuleButton("ToggleButton", 183, "FREECAM: OFF (ALT)")
toggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)

---------------------------------------------------------
-- SETTINGS MENU / DEVELOPER CONFIG TAB
---------------------------------------------------------
local settingsFrame = Instance.new("Frame")
settingsFrame.Name = "SettingsFrame"
settingsFrame.Size = UDim2.new(0, 420, 0, 410)
settingsFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
settingsFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
settingsFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
settingsFrame.BorderSizePixel = 0
settingsFrame.Active = true
settingsFrame.Draggable = true
settingsFrame.Visible = false
settingsFrame.ZIndex = 20
settingsFrame.Parent = screenGui

local settingsCorner = Instance.new("UICorner")
settingsCorner.CornerRadius = UDim.new(0, 10)
settingsCorner.Parent = settingsFrame

local settingsStroke = Instance.new("UIStroke")
settingsStroke.Transparency = 0.4
settingsStroke.Thickness = 1.5
settingsStroke.Parent = settingsFrame

local settingsStrokeGradient = Instance.new("UIGradient")
settingsStrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 80, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 100, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 200, 255))
})
settingsStrokeGradient.Parent = settingsStroke

task.spawn(function()
    local offset = 0
    while true do
        offset = (offset + 0.01) % 1
        settingsStrokeGradient.Offset = Vector2.new(offset, 0)
        RunService.RenderStepped:Wait()
    end
end)

local settingsTitle = Instance.new("TextLabel")
settingsTitle.Size = UDim2.new(1, -40, 0, 35)
settingsTitle.Position = UDim2.new(0, 12, 0, 0)
settingsTitle.BackgroundTransparency = 1
settingsTitle.Text = "NEXO // Config & Console"
settingsTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
settingsTitle.Font = Enum.Font.GothamBold
settingsTitle.TextSize = 15
settingsTitle.TextXAlignment = Enum.TextXAlignment.Left
settingsTitle.ZIndex = 21
settingsTitle.Parent = settingsFrame

local settingsClose = Instance.new("TextButton")
settingsClose.Size = UDim2.new(0, 25, 0, 25)
settingsClose.Position = UDim2.new(1, -28, 0, 5)
settingsClose.BackgroundTransparency = 1
settingsClose.Text = "✕"
settingsClose.TextColor3 = Color3.fromRGB(255, 90, 110)
settingsClose.Font = Enum.Font.GothamBold
settingsClose.TextSize = 14
settingsClose.ZIndex = 21
settingsClose.Parent = settingsFrame

-- Config Container (Left Side of Settings)
local configContainer = Instance.new("ScrollingFrame")
configContainer.Size = UDim2.new(0.45, -5, 1, -125)
configContainer.Position = UDim2.new(0, 10, 0, 40)
configContainer.BackgroundTransparency = 1
configContainer.BorderSizePixel = 0
configContainer.ScrollBarThickness = 4
configContainer.CanvasSize = UDim2.new(0, 0, 0, 500)
configContainer.ZIndex = 21
configContainer.Parent = settingsFrame

local configLayout = Instance.new("UIListLayout")
configLayout.Padding = UDim.new(0, 6)
configLayout.SortOrder = Enum.SortOrder.LayoutOrder
configLayout.Parent = configContainer

local settingTextBoxes = {}
local keybindButtonRefs = {}

local function updateButtonTexts()
    startButton.Text = "Start TP Loop (" .. string.upper(CONFIG.KEY_TP_LOOP.Name) .. ")"
    antiMurderBtn.Text = "Anti-Murder: " .. (isAntiMurderActive and "ON" or "OFF") .. " (" .. string.upper(CONFIG.KEY_ANTI_MURDER.Name) .. ")"
    murderKAButton.Text = "Murder KA: " .. (isMurderKillAuraActive and "ON" or "OFF") .. " (" .. string.upper(CONFIG.KEY_MURDER_KA.Name) .. ")"
    sheriffKAButton.Text = "Sheriff KA: " .. (isSheriffKillAuraActive and "ON" or "OFF") .. " (" .. string.upper(CONFIG.KEY_SHERIFF_KA.Name) .. ")"
    toggleButton.Text = "FREECAM: " .. (isFreecamActive and "ON" or "OFF") .. " (" .. string.upper(CONFIG.KEY_FREECAM.Name) .. ")"
end

local function createConfigInput(configKey, nameText, defaultVal, callback)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 40)
    row.BackgroundTransparency = 1
    row.ZIndex = 22

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = nameText
    label.TextColor3 = Color3.fromRGB(200, 200, 210)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.ZIndex = 22
    label.Parent = row

    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0.4, 0, 0.8, 0)
    box.Position = UDim2.new(0.6, 0, 0.1, 0)
    box.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
    box.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.Text = tostring(defaultVal)
    box.Font = Enum.Font.GothamBold
    box.TextSize = 12
    box.ZIndex = 22
    box.Parent = row

    local boxCorner = Instance.new("UICorner")
    boxCorner.CornerRadius = UDim.new(0, 6)
    boxCorner.Parent = box

    settingTextBoxes[configKey] = box

    box.FocusLost:Connect(function()
        local num = tonumber(box.Text)
        if num then
            callback(num)
        else
            box.Text = tostring(CONFIG[configKey])
        end
    end)

    row.Parent = configContainer
end

local function createKeybindInput(configKey, nameText)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 40)
    row.BackgroundTransparency = 1
    row.ZIndex = 22

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = nameText
    label.TextColor3 = Color3.fromRGB(200, 200, 210)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.ZIndex = 22
    label.Parent = row

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.5, 0, 0.8, 0)
    btn.Position = UDim2.new(0.5, 0, 0.1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(35, 30, 50)
    btn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    btn.TextColor3 = Color3.fromRGB(200, 170, 255)
    btn.Text = CONFIG[configKey].Name
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    btn.ZIndex = 22
    btn.Parent = row

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn

    keybindButtonRefs[configKey] = btn

    btn.MouseButton1Click:Connect(function()
        if listeningForKeyConfig then return end
        listeningForKeyConfig = configKey
        btn.Text = "Press key..."
        btn.TextColor3 = Color3.fromRGB(255, 200, 80)

        local connection
        connection = UserInputService.InputBegan:Connect(function(input, gameProcessed)
            if input.UserInputType == Enum.UserInputType.Keyboard then
                CONFIG[configKey] = input.KeyCode
                btn.Text = input.KeyCode.Name
                btn.TextColor3 = Color3.fromRGB(200, 170, 255)
                listeningForKeyConfig = nil
                updateButtonTexts()
                connection:Disconnect()
            end
        end)
    end)

    row.Parent = configContainer
end

-- Reset Settings Button
local resetSettingsBtn = Instance.new("TextButton")
resetSettingsBtn.Size = UDim2.new(0.45, -5, 0, 32)
resetSettingsBtn.Position = UDim2.new(0, 10, 1, -75)
resetSettingsBtn.BackgroundColor3 = Color3.fromRGB(50, 25, 30)
resetSettingsBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
resetSettingsBtn.Text = "↺ Reset Settings"
resetSettingsBtn.TextColor3 = Color3.fromRGB(255, 120, 130)
resetSettingsBtn.Font = Enum.Font.GothamBold
resetSettingsBtn.TextSize = 12
resetSettingsBtn.ZIndex = 21
resetSettingsBtn.Parent = settingsFrame

local resetCorner = Instance.new("UICorner")
resetCorner.CornerRadius = UDim.new(0, 6)
resetCorner.Parent = resetSettingsBtn

local resetStroke = Instance.new("UIStroke")
resetStroke.Color = Color3.fromRGB(255, 90, 110)
resetStroke.Transparency = 0.6
resetStroke.Parent = resetSettingsBtn

resetSettingsBtn.MouseEnter:Connect(function()
    TweenService:Create(resetSettingsBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(70, 30, 40), BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY - 0.05}):Play()
    TweenService:Create(resetStroke, TweenInfo.new(0.2), {Transparency = 0.2}):Play()
end)

resetSettingsBtn.MouseLeave:Connect(function()
    TweenService:Create(resetSettingsBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 25, 30), BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY}):Play()
    TweenService:Create(resetStroke, TweenInfo.new(0.2), {Transparency = 0.6}):Play()
end)

-- Console Scrolling Frame for Logs
local consoleScroll = Instance.new("ScrollingFrame")
consoleScroll.Size = UDim2.new(0.53, -5, 1, -85)
consoleScroll.Position = UDim2.new(0.47, 0, 0, 40)
consoleScroll.BackgroundColor3 = Color3.fromRGB(12, 12, 16)
consoleScroll.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
consoleScroll.BorderSizePixel = 0
consoleScroll.ScrollBarThickness = 6
consoleScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
consoleScroll.ZIndex = 21
consoleScroll.Parent = settingsFrame

local consoleCorner = Instance.new("UICorner")
consoleCorner.CornerRadius = UDim.new(0, 6)
consoleCorner.Parent = consoleScroll

local consoleLayout = Instance.new("UIListLayout")
consoleLayout.Padding = UDim.new(0, 2)
consoleLayout.SortOrder = Enum.SortOrder.LayoutOrder
consoleLayout.Parent = consoleScroll

consoleLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    consoleScroll.CanvasSize = UDim2.new(0, 0, 0, consoleLayout.AbsoluteContentSize.Y)
end)

local function logToConsole(message, messageType)
    local logRow = Instance.new("TextLabel")
    logRow.Size = UDim2.new(1, -5, 0, 18)
    logRow.BackgroundTransparency = 1
    logRow.Text = "[" .. os.strftime("%H:%M:%S") .. "] " .. message
    logRow.TextXAlignment = Enum.TextXAlignment.Left
    logRow.Font = Enum.Font.Code
    logRow.TextSize = 11
    logRow.ZIndex = 22
    
    if messageType == Enum.MessageType.MessageOutput then
        logRow.TextColor3 = Color3.fromRGB(180, 180, 190)
    elseif messageType == Enum.MessageType.MessageWarning then
        logRow.TextColor3 = Color3.fromRGB(255, 200, 80)
    elseif messageType == Enum.MessageType.MessageError then
        logRow.TextColor3 = Color3.fromRGB(255, 90, 110)
    else
        logRow.TextColor3 = Color3.fromRGB(140, 120, 255)
    end
    
    logRow.Parent = consoleScroll
    consoleScroll.CanvasPosition = Vector2.new(0, consoleLayout.AbsoluteContentSize.Y)
end

LogService.MessageOut:Connect(function(message, messageType)
    logToConsole(message, messageType)
end)

local commandInput = Instance.new("TextBox")
commandInput.Size = UDim2.new(1, -20, 0, 35)
commandInput.Position = UDim2.new(0, 10, 1, -40)
commandInput.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
commandInput.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
commandInput.TextColor3 = Color3.fromRGB(255, 255, 255)
commandInput.PlaceholderText = "Type command here..."
commandInput.PlaceholderColor3 = Color3.fromRGB(120, 120, 140)
commandInput.Text = ""
commandInput.Font = Enum.Font.Code
commandInput.TextSize = 12
commandInput.ZIndex = 21
commandInput.Parent = settingsFrame

local cmdCorner = Instance.new("UICorner")
cmdCorner.CornerRadius = UDim.new(0, 6)
cmdCorner.Parent = commandInput

---------------------------------------------------------
-- FORWARD DECLARATIONS
---------------------------------------------------------
local startTeleportSequence, disableFreecam, enableFreecam

---------------------------------------------------------
-- COMMAND PROCESSOR
---------------------------------------------------------
local function processCommand(text)
    text = string.lower(string.match(text, "^%s*(.-)%s*$"))
    if text == "" then return end

    logToConsole("> " .. text, Enum.MessageType.MessageInfo)

    local parts = {}
    for word in string.gmatch(text, "%S+") do
        table.insert(parts, word)
    end

    local cmd = parts[1]

    if cmd == "toggle" then
        local moduleName = parts[2]
        if not moduleName then
            logToConsole("Usage: toggle [antimurder/murderka/sheriffka/freecam/tploop]", Enum.MessageType.MessageWarning)
            return
        end

        if moduleName == "antimurder" or moduleName == "am" then
            isAntiMurderActive = not isAntiMurderActive
            antiMurderBtn.BackgroundColor3 = isAntiMurderActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
        elseif moduleName == "murderka" or moduleName == "mka" then
            isMurderKillAuraActive = not isMurderKillAuraActive
            murderKAButton.BackgroundColor3 = isMurderKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
        elseif moduleName == "sheriffka" or moduleName == "ska" then
            isSheriffKillAuraActive = not isSheriffKillAuraActive
            sheriffKAButton.BackgroundColor3 = isSheriffKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
        elseif moduleName == "freecam" or moduleName == "fc" then
            if isFreecamActive then disableFreecam() else enableFreecam() end
        elseif moduleName == "tploop" or moduleName == "tp" then
            if not isTeleportRunning then task.spawn(startTeleportSequence) else isTeleportRunning = false end
        else
            logToConsole("Unknown module: " .. moduleName, Enum.MessageType.MessageError)
        end
        updateButtonTexts()

    elseif cmd == "set" then
        local settingName = parts[2]
        local val = tonumber(parts[3])

        if not settingName or not val then
            logToConsole("Usage: set [murderka/sheriffka/antimurder/height/speed/gui_trans/elem_trans] [value]", Enum.MessageType.MessageWarning)
            return
        end

        if settingName == "murderka" or settingName == "mka" then
            CONFIG.MURDER_KA_DISTANCE = val
            if settingTextBoxes["MURDER_KA_DISTANCE"] then settingTextBoxes["MURDER_KA_DISTANCE"].Text = tostring(val) end
        elseif settingName == "sheriffka" or settingName == "ska" then
            CONFIG.SHERIFF_KA_DISTANCE = val
            if settingTextBoxes["SHERIFF_KA_DISTANCE"] then settingTextBoxes["SHERIFF_KA_DISTANCE"].Text = tostring(val) end
        elseif settingName == "antimurder" or settingName == "am" then
            CONFIG.ANTI_MURDER_DISTANCE = val
            if settingTextBoxes["ANTI_MURDER_DISTANCE"] then settingTextBoxes["ANTI_MURDER_DISTANCE"].Text = tostring(val) end
        elseif settingName == "height" or settingName == "sky" then
            CONFIG.ANTI_MURDER_SKY_HEIGHT = val
            if settingTextBoxes["ANTI_MURDER_SKY_HEIGHT"] then settingTextBoxes["ANTI_MURDER_SKY_HEIGHT"].Text = tostring(val) end
        elseif settingName == "speed" then
            CONFIG.MOVE_SPEED = val
            if settingTextBoxes["MOVE_SPEED"] then settingTextBoxes["MOVE_SPEED"].Text = tostring(val) end
        elseif settingName == "gui_trans" then
            CONFIG.GUI_TRANSPARENCY = math.clamp(val, 0, 1)
            mainFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
            settingsFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
            if settingTextBoxes["GUI_TRANSPARENCY"] then settingTextBoxes["GUI_TRANSPARENCY"].Text = tostring(CONFIG.GUI_TRANSPARENCY) end
        elseif settingName == "elem_trans" then
            CONFIG.ELEMENT_TRANSPARENCY = math.clamp(val, 0, 1)
            settingsButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            antiMurderBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            murderKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            sheriffKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            toggleButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            consoleScroll.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            commandInput.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
            if settingTextBoxes["ELEMENT_TRANSPARENCY"] then settingTextBoxes["ELEMENT_TRANSPARENCY"].Text = tostring(CONFIG.ELEMENT_TRANSPARENCY) end
        else
            logToConsole("Unknown setting: " .. settingName, Enum.MessageType.MessageError)
        end
    elseif cmd == "reset" then
        for k, v in pairs(DEFAULT_CONFIG) do
            CONFIG[k] = v
            if settingTextBoxes[k] then
                settingTextBoxes[k].Text = tostring(v)
            end
            if keybindButtonRefs[k] then
                keybindButtonRefs[k].Text = v.Name
            end
        end
        updateButtonTexts()
        mainFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
        settingsFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
        settingsButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        antiMurderBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        murderKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        sheriffKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        toggleButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        consoleScroll.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        commandInput.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        logToConsole("All settings reset to defaults via command.", Enum.MessageType.MessageInfo)
    elseif cmd == "help" then
        logToConsole("Commands: toggle [antimurder/murderka/sheriffka/freecam/tploop]", Enum.MessageType.MessageInfo)
        logToConsole("Commands: set [murderka/sheriffka/antimurder/height/speed/gui_trans/elem_trans] [val]", Enum.MessageType.MessageInfo)
        logToConsole("Commands: reset", Enum.MessageType.MessageInfo)
    else
        logToConsole("Unknown command. Type 'help' for info.", Enum.MessageType.MessageError)
    end
end

commandInput.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        processCommand(commandInput.Text)
        commandInput.Text = ""
    end
end)

settingsButton.MouseButton1Click:Connect(function() 
    settingsFrame.Visible = not settingsFrame.Visible
    if settingsFrame.Visible then
        settingsFrame.Size = UDim2.new(0, 0, 0, 0)
        TweenService:Create(settingsFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 420, 0, 410)}):Play()
    end
end)
settingsClose.MouseButton1Click:Connect(function() settingsFrame.Visible = false end)

-- Reset Button Click Event
resetSettingsBtn.MouseButton1Click:Connect(function()
    for k, v in pairs(DEFAULT_CONFIG) do
        CONFIG[k] = v
        if settingTextBoxes[k] then
            settingTextBoxes[k].Text = tostring(v)
        end
        if keybindButtonRefs[k] then
            keybindButtonRefs[k].Text = v.Name
        end
    end
    updateButtonTexts()
    mainFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
    settingsFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
    settingsButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    antiMurderBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    murderKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    sheriffKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    toggleButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    consoleScroll.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    commandInput.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    
    logToConsole("Settings successfully reset to factory defaults.", Enum.MessageType.MessageInfo)
end)

-- Spectate Bar
local specFrame = Instance.new("Frame")
specFrame.Size = UDim2.new(0, 300, 0, 45)
specFrame.Position = UDim2.new(0.5, -150, 0.88, 0)
specFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
specFrame.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
specFrame.BorderSizePixel = 0
specFrame.Visible = false
specFrame.ZIndex = 10
specFrame.Parent = screenGui

local specCorner = Instance.new("UICorner")
specCorner.CornerRadius = UDim.new(0, 10)
specCorner.Parent = specFrame

local specStroke = Instance.new("UIStroke")
specStroke.Transparency = 0.4
specStroke.Parent = specFrame

local specStrokeGradient = Instance.new("UIGradient")
specStrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 80, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 100, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 200, 255))
})
specStrokeGradient.Parent = specStroke

task.spawn(function()
    local offset = 0
    while true do
        offset = (offset + 0.01) % 1
        specStrokeGradient.Offset = Vector2.new(offset, 0)
        RunService.RenderStepped:Wait()
    end
end)

local prevBtn = Instance.new("TextButton")
prevBtn.Size = UDim2.new(0, 40, 1, -10)
prevBtn.Position = UDim2.new(0, 5, 0, 5)
prevBtn.Text = "<"
prevBtn.Font = Enum.Font.GothamBold
prevBtn.TextSize = 18
prevBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
prevBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
prevBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
prevBtn.ZIndex = 11
prevBtn.Parent = specFrame

local prevCorner = Instance.new("UICorner")
prevCorner.CornerRadius = UDim.new(0, 6)
prevCorner.Parent = prevBtn

local nextBtn = Instance.new("TextButton")
nextBtn.Size = UDim2.new(0, 40, 1, -10)
nextBtn.Position = UDim2.new(1, -45, 0, 5)
nextBtn.Text = ">"
nextBtn.Font = Enum.Font.GothamBold
nextBtn.TextSize = 18
nextBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
nextBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
nextBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
nextBtn.ZIndex = 11
nextBtn.Parent = specFrame

local nextCorner = Instance.new("UICorner")
nextCorner.CornerRadius = UDim.new(0, 6)
nextCorner.Parent = nextBtn

local nameLabel = Instance.new("TextLabel")
nameLabel.Size = UDim2.new(1, -100, 1, -10)
nameLabel.Position = UDim2.new(0, 50, 0, 5)
nameLabel.BackgroundTransparency = 1
nameLabel.Font = Enum.Font.GothamBold
nameLabel.TextSize = 14
nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
nameLabel.Text = "Spectating: None"
nameLabel.ZIndex = 11
nameLabel.Parent = specFrame

---------------------------------------------------------
-- PLAYER LIST HEADER FRAME (ROLE ESP LIST)
---------------------------------------------------------
local listFrame = Instance.new("Frame")
listFrame.Name = "PlayerListFrame"
listFrame.Size = UDim2.new(0, 220, 0, 250)
listFrame.Position = UDim2.new(0.02, 0, 0.53, 0)
listFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
listFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
listFrame.BorderSizePixel = 0
listFrame.Active = true
listFrame.Draggable = true
listFrame.ZIndex = 10
listFrame.Parent = screenGui

local listCorner = Instance.new("UICorner")
listCorner.CornerRadius = UDim.new(0, 10)
listCorner.Parent = listFrame

local listStroke = Instance.new("UIStroke")
listStroke.Name = "AnimatedListStroke"
listStroke.Transparency = 0.4
listStroke.Thickness = 1.5
listStroke.Parent = listFrame

local listStrokeGradient = Instance.new("UIGradient")
listStrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(120, 80, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 100, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 200, 255))
})
listStrokeGradient.Parent = listStroke

task.spawn(function()
    local offset = 0
    while true do
        offset = (offset + 0.01) % 1
        listStrokeGradient.Offset = Vector2.new(offset, 0)
        
        local tween1 = TweenService:Create(listStroke, TweenInfo.new(2.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.15, Thickness = 2.0})
        tween1:Play()
        tween1.Completed:Wait()
        local tween2 = TweenService:Create(listStroke, TweenInfo.new(2.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.6, Thickness = 1.2})
        tween2:Play()
        tween2.Completed:Wait()
    end
end)

local listTitle = Instance.new("TextLabel")
listTitle.Name = "Header"
listTitle.Size = UDim2.new(1, 0, 0, 30)
listTitle.Position = UDim2.new(0, 0, 0, 0)
listTitle.BackgroundTransparency = 1
listTitle.Text = "NEXO // Player Roles"
listTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
listTitle.Font = Enum.Font.GothamBold
listTitle.TextSize = 14
listTitle.ZIndex = 11
listTitle.Parent = listFrame

local listTitleGradient = Instance.new("UIGradient")
listTitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(150, 120, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})
listTitleGradient.Parent = listTitle

task.spawn(function()
    local offset = 0
    while true do
        offset = (offset - 0.006) % 1
        listTitleGradient.Offset = Vector2.new(offset, 0)
        RunService.RenderStepped:Wait()
    end
end)

local scrollContainer = Instance.new("ScrollingFrame")
scrollContainer.Size = UDim2.new(1, -10, 1, -35)
scrollContainer.Position = UDim2.new(0, 5, 0, 30)
scrollContainer.BackgroundTransparency = 1
scrollContainer.BorderSizePixel = 0
scrollContainer.ScrollBarThickness = 4
scrollContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollContainer.ZIndex = 11
scrollContainer.Parent = listFrame

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 4)
listLayout.SortOrder = Enum.SortOrder.Name
listLayout.Parent = scrollContainer

listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    scrollContainer.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y)
end)

-- Fill Settings Config Fields & Keybind Rebinders
createKeybindInput("KEY_TP_LOOP", "TP Loop Key:")
createKeybindInput("KEY_ANTI_MURDER", "Anti-Murder Key:")
createKeybindInput("KEY_MURDER_KA", "Murder KA Key:")
createKeybindInput("KEY_SHERIFF_KA", "Sheriff KA Key:")
createKeybindInput("KEY_FREECAM", "Freecam Key:")

createConfigInput("MURDER_KA_DISTANCE", "Murder KA Dist:", CONFIG.MURDER_KA_DISTANCE, function(v) CONFIG.MURDER_KA_DISTANCE = v end)
createConfigInput("SHERIFF_KA_DISTANCE", "Sheriff KA Dist:", CONFIG.SHERIFF_KA_DISTANCE, function(v) CONFIG.SHERIFF_KA_DISTANCE = v end)
createConfigInput("ANTI_MURDER_DISTANCE", "Anti-Murder Dist:", CONFIG.ANTI_MURDER_DISTANCE, function(v) CONFIG.ANTI_MURDER_DISTANCE = v end)
createConfigInput("ANTI_MURDER_SKY_HEIGHT", "Anti-Murder Height:", CONFIG.ANTI_MURDER_SKY_HEIGHT, function(v) CONFIG.ANTI_MURDER_SKY_HEIGHT = v end)
createConfigInput("MOVE_SPEED", "Freecam Speed:", CONFIG.MOVE_SPEED, function(v) CONFIG.MOVE_SPEED = v end)
createConfigInput("GUI_TRANSPARENCY", "GUI Transparency:", CONFIG.GUI_TRANSPARENCY, function(v) 
    CONFIG.GUI_TRANSPARENCY = math.clamp(v, 0, 1)
    mainFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
    settingsFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
    listFrame.BackgroundTransparency = CONFIG.GUI_TRANSPARENCY
end)
createConfigInput("ELEMENT_TRANSPARENCY", "Elem Transparency:", CONFIG.ELEMENT_TRANSPARENCY, function(v)
    CONFIG.ELEMENT_TRANSPARENCY = math.clamp(v, 0, 1)
    settingsButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    antiMurderBtn.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    murderKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    sheriffKAButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    toggleButton.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    consoleScroll.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
    commandInput.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
end)

---------------------------------------------------------
-- HELPER FUNCTIONS & ROLE SCANNER
---------------------------------------------------------
local function getPlayerRole(player)
    if not player then return "Innocent", COLOR_INNOCENT end
    local hasGun, hasKnifeRole = false, false

    local function scanTools(container)
        if not container then return end
        for _, child in ipairs(container:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "gun" then hasGun = true
                elseif name == "knife" or name == "kinfe" then hasKnifeRole = true end
            end
        end
    end

    scanTools(player.Character)
    scanTools(player:FindFirstChild("Backpack"))

    if hasKnifeRole then return "Murder", COLOR_MURDER
    elseif hasGun then return "Sheriff", COLOR_SHERIFF
    else return "Innocent", COLOR_INNOCENT end
end

local function applyHighlight(player, roleText, roleColor)
    local char = player.Character
    if not char then return end
    local existingHighlight = char:FindFirstChild("RoleHighlight")

    if roleText == "Sheriff" or roleText == "Murder" then
        if not existingHighlight then
            existingHighlight = Instance.new("Highlight")
            existingHighlight.Name = "RoleHighlight"
            existingHighlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            existingHighlight.FillTransparency = 0.5
            existingHighlight.OutlineTransparency = 0
            existingHighlight.Parent = char
        end
        existingHighlight.FillColor = roleColor
        existingHighlight.OutlineColor = roleColor
    else
        if existingHighlight then existingHighlight:Destroy() end
    end
end

local function updatePlayerList()
    for _, child in ipairs(scrollContainer:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end

    for _, p in ipairs(Players:GetPlayers()) do
        local roleText, roleColor = getPlayerRole(p)
        applyHighlight(p, roleText, roleColor)

        local rowFrame = Instance.new("Frame")
        rowFrame.Name = p.Name
        rowFrame.Size = UDim2.new(1, -6, 0, 22)
        rowFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
        rowFrame.BackgroundTransparency = CONFIG.ELEMENT_TRANSPARENCY
        rowFrame.BorderSizePixel = 0
        rowFrame.ZIndex = 12

        local rowCorner = Instance.new("UICorner")
        rowCorner.CornerRadius = UDim.new(0, 5)
        rowCorner.Parent = rowFrame

        local pNameLabel = Instance.new("TextLabel")
        pNameLabel.Size = UDim2.new(0.6, 0, 1, 0)
        pNameLabel.Position = UDim2.new(0, 6, 0, 0)
        pNameLabel.BackgroundTransparency = 1
        pNameLabel.Text = p.DisplayName
        pNameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        pNameLabel.TextXAlignment = Enum.TextXAlignment.Left
        pNameLabel.Font = Enum.Font.Gotham
        pNameLabel.TextSize = 12
        pNameLabel.ZIndex = 13
        pNameLabel.Parent = rowFrame

        local roleLabel = Instance.new("TextLabel")
        roleLabel.Size = UDim2.new(0.4, -6, 1, 0)
        roleLabel.Position = UDim2.new(0.6, 0, 0, 0)
        roleLabel.BackgroundTransparency = 1
        roleLabel.Text = roleText
        roleLabel.TextColor3 = roleColor
        roleLabel.TextXAlignment = Enum.TextXAlignment.Right
        roleLabel.Font = Enum.Font.GothamBold
        roleLabel.TextSize = 12
        roleLabel.ZIndex = 13
        roleLabel.Parent = rowFrame

        rowFrame.Parent = scrollContainer
    end
end

task.spawn(function()
    while true do
        updatePlayerList()
        task.wait(1)
    end
end)

local function getEquippedToolType(character)
    if not character then return "None" end
    for _, child in ipairs(character:GetChildren()) do
        if child:IsA("Tool") then
            local name = string.lower(child.Name)
            if name == "gun" then return "Gun"
            elseif name == "knife" or name == "kinfe" then return "Knife" end
        end
    end
    return "None"
end

local function hasKnife(player)
    if player.Character then
        for _, child in ipairs(player.Character:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "knife" or name == "kinfe" then return true end
            end
        end
    end
    local backpack = player:FindFirstChild("Backpack")
    if backpack then
        for _, child in ipairs(backpack:GetChildren()) do
            if child:IsA("Tool") then
                local name = string.lower(child.Name)
                if name == "knife" or name == "kinfe" then return true end
            end
        end
    end
    return false
end

local function simulateLeftClick()
    local viewport = workspace.CurrentCamera.ViewportSize
    VirtualInputManager:SendMouseButtonEvent(viewport.X / 2, viewport.Y / 2, 0, true, game, 0)
    task.wait(0.01)
    VirtualInputManager:SendMouseButtonEvent(viewport.X / 2, viewport.Y / 2, 0, false, game, 0)
end

local function clickOnPosition(worldPos)
    local screenPos, onScreen = camera:WorldToViewportPoint(worldPos)
    if onScreen then
        VirtualInputManager:SendMouseButtonEvent(screenPos.X, screenPos.Y, 0, true, game, 0)
        task.wait(0.02)
        VirtualInputManager:SendMouseButtonEvent(screenPos.X, screenPos.Y, 0, false, game, 0)
    end
end

---------------------------------------------------------
-- EXISTING GAMEPLAY LOOPS
---------------------------------------------------------
task.spawn(function()
    while true do
        task.wait(0.5)
        if isTeleportRunning then
            local character = localPlayer.Character
            if character then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if humanoid and hrp then
                    local randomOffset = Vector3.new(math.random(-30, 30), 0, math.random(-30, 30))
                    humanoid:MoveTo(hrp.Position + randomOffset)
                end
            end
        end
    end
end)

local function isVisibleToPlayer(targetPart)
    local char = localPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
    local myPos = char.HumanoidRootPart.Position
    local targetPos = targetPart.Position
    if (targetPos - myPos).Magnitude > 60 then return false end
    
    local _, onScreen = camera:WorldToViewportPoint(targetPos)
    if not onScreen then return false end
    
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {char, targetPart.Parent}
    raycastParams.IgnoreWater = true
    
    local rayResult = workspace:Raycast(camera.CFrame.Position, targetPos - camera.CFrame.Position, raycastParams)
    return rayResult == nil
end

-- Anti-Murder Loop
task.spawn(function()
    while true do
        task.wait(0.1)
        if isAntiMurderActive then
            local myChar = localPlayer.Character
            if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                local myHRP = myChar.HumanoidRootPart
                local myPos = myHRP.Position
                
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= localPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        local role, _ = getPlayerRole(p)
                        if role == "Murder" then
                            local targetPart = p.Character.HumanoidRootPart
                            if (targetPart.Position - myPos).Magnitude <= CONFIG.ANTI_MURDER_DISTANCE and isVisibleToPlayer(targetPart) then
                                myHRP.CFrame = myHRP.CFrame * CFrame.new(0, CONFIG.ANTI_MURDER_SKY_HEIGHT, 0)
                                task.wait(2)
                                break
                            end
                        end
                    end
                end
            end
        end
    end
end)

-- Murderer Kill Aura
task.spawn(function()
    while true do
        task.wait(0.05)
        if isMurderKillAuraActive then
            local myChar = localPlayer.Character
            if myChar and myChar:FindFirstChild("HumanoidRootPart") and getPlayerRole(localPlayer) == "Murder" then
                local myHrp = myChar.HumanoidRootPart
                for _, targetP in ipairs(Players:GetPlayers()) do
                    if targetP ~= localPlayer and targetP.Character and targetP.Character:FindFirstChild("HumanoidRootPart") then
                        local targetHrp = targetP.Character.HumanoidRootPart
                        if (targetHrp.Position - myHrp.Position).Magnitude <= CONFIG.MURDER_KA_DISTANCE then
                            if getEquippedToolType(myChar) ~= "Knife" then
                                local backpack = localPlayer:FindFirstChild("Backpack")
                                if backpack then
                                    for _, tool in ipairs(backpack:GetChildren()) do
                                        if tool:IsA("Tool") and (string.lower(tool.Name) == "knife" or string.lower(tool.Name) == "kinfe") then
                                            tool.Parent = myChar
                                            break
                                        end
                                    end
                                end
                            end
                            local originalCFrame = myHrp.CFrame
                            local attackStart = os.clock()
                            while isMurderKillAuraActive and targetP.Character and targetHrp.Parent and (os.clock() - attackStart < CONFIG.ATTACK_DURATION) do
                                myHrp.CFrame = targetHrp.CFrame * CFrame.new(0, 0, 1.5)
                                simulateLeftClick()
                                RunService.Heartbeat:Wait()
                            end
                            myHrp.CFrame = originalCFrame
                            task.wait(0.2)
                            break
                        end
                    end
                end
            end
        end
    end
end)

-- Sheriff Kill Aura
task.spawn(function()
    while true do
        task.wait(0.05)
        if isSheriffKillAuraActive then
            local myChar = localPlayer.Character
            if myChar and myChar:FindFirstChild("HumanoidRootPart") and getPlayerRole(localPlayer) == "Sheriff" then
                local myHrp = myChar.HumanoidRootPart
                for _, targetP in ipairs(Players:GetPlayers()) do
                    if targetP ~= localPlayer and targetP.Character and targetP.Character:FindFirstChild("HumanoidRootPart") then
                        if getPlayerRole(targetP) == "Murder" then
                            local targetHrp = targetP.Character.HumanoidRootPart
                            if (targetHrp.Position - myHrp.Position).Magnitude <= CONFIG.SHERIFF_KA_DISTANCE then
                                clickOnPosition(targetHrp.Position)
                                task.wait(0.1)
                            end
                        end
                    end
                end
            end
        end
    end
end)

function startTeleportSequence()
    if isTeleportRunning then return end
    local myCharacter = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local myHRP = myCharacter:WaitForChild("HumanoidRootPart")
    local toolType = getEquippedToolType(myCharacter)

    isTeleportRunning = true
    startButton.Text = "Running... (" .. string.upper(CONFIG.KEY_TP_LOOP.Name) .. ")"
    startButton.BackgroundColor3 = Color3.fromRGB(30, 45, 40)

    if toolType == "None" then
        myHRP.CFrame = myHRP.CFrame * CFrame.new(0, 100, 0)
        task.wait(5)
    else
        local visited = {}
        for _, targetP in ipairs(Players:GetPlayers()) do
            if getEquippedToolType(myCharacter) == "None" or not isTeleportRunning then break end
            if targetP ~= localPlayer and not visited[targetP] then
                visited[targetP] = true
                if getEquippedToolType(myCharacter) == "Knife" or hasKnife(targetP) then
                    local targetChar = targetP.Character
                    if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                        local targetHRP = targetChar.HumanoidRootPart
                        local startTime = os.clock()
                        local lastClick = 0
                        while isTeleportRunning and (os.clock() - startTime < CONFIG.ATTACK_DURATION) do
                            if getEquippedToolType(myCharacter) == "None" or not targetChar or not targetHRP.Parent then break end
                            myHRP.CFrame = targetHRP.CFrame
                            if os.clock() - lastClick >= CONFIG.CLICK_INTERVAL then
                                simulateLeftClick()
                                lastClick = os.clock()
                            end
                            RunService.Heartbeat:Wait()
                        end
                    end
                end
            end
        end
    end

    isTeleportRunning = false
    updateButtonTexts()
    startButton.BackgroundColor3 = Color3.fromRGB(20, 45, 35)
end

---------------------------------------------------------
-- FREECAM LOGIC
---------------------------------------------------------
local function updateButtonUI()
    if isFreecamActive then
        toggleButton.BackgroundColor3 = Color3.fromRGB(20, 45, 35)
        toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        specFrame.Visible = true
    else
        toggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        toggleButton.TextColor3 = Color3.fromRGB(200, 200, 210)
        specFrame.Visible = false
    end
    updateButtonTexts()
end

local function getSpectateTargets()
    local targets = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= localPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
            table.insert(targets, p)
        end
    end
    return targets
end

local function setSpectateTarget(index)
    local targets = getSpectateTargets()
    if #targets == 0 then
        isSpectating = false
        targetPlayer = nil
        nameLabel.Text = "Freecam Mode (No Players)"
        return
    end
    if index > #targets then index = 1 end
    if index < 1 then index = #targets end
    spectateIndex = index
    targetPlayer = targets[spectateIndex]
    isSpectating = true
    nameLabel.Text = "Spectating: " .. targetPlayer.DisplayName
end

local function createGhostModel(originalChar)
    local hrp = originalChar:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local ghost = Instance.new("Model")
    ghost.Name = "GhostAvatar"
    local ghostHrp = hrp:Clone()
    ghostHrp.Transparency = 0.1
    ghostHrp.Color = Color3.fromRGB(150, 120, 255)
    ghostHrp.Material = Enum.Material.ForceField
    ghostHrp.CanCollide = false
    ghostHrp.Anchored = true
    ghostHrp.Parent = ghost
    ghost.PrimaryPart = ghostHrp
    ghost.Parent = workspace
    return ghost
end

local function freezeCharacterMovement()
    local character = localPlayer.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then humanoid:Move(Vector3.zero, true) end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.AssemblyLinearVelocity = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
    end
end

local function restoreCameraToPlayer()
    local character = localPlayer.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if humanoid then
        camera.CameraType = Enum.CameraType.Track
        camera.CameraSubject = humanoid
        if hrp then camera.CFrame = hrp.CFrame * CFrame.new(0, 2, 8) end
        task.defer(function()
            camera.CameraType = Enum.CameraType.Custom
            camera.CameraSubject = humanoid
        end)
    end
end

function disableFreecam()
    isFreecamActive = false
    isSpectating = false
    isRightMouseDown = false
    targetPlayer = nil
    if renderConnection then renderConnection:Disconnect(); renderConnection = nil end
    pcall(function() ContextActionService:UnbindAction(ACTION_FREEZE) end)
    if ghostCharacter then ghostCharacter:Destroy(); ghostCharacter = nil end
    restoreCameraToPlayer()
    UserInputService.MouseBehavior = Enum.MouseBehavior.Default
    updateButtonUI()
end

local function teleportToFreecam()
    if not isFreecamActive then return end
    local targetCFrame = camera.CFrame
    local character = localPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = targetCFrame
    end
    disableFreecam()
end

function enableFreecam()
    if isFreecamActive then return end
    local realCharacter = localPlayer.Character
    if not realCharacter or not realCharacter:FindFirstChild("HumanoidRootPart") then return end

    isFreecamActive = true
    isSpectating = false
    targetPlayer = nil
    nameLabel.Text = "Free Flying"
    updateButtonUI()
    freezeCharacterMovement()

    pcall(function()
        ContextActionService:BindActionAtPriority(
            ACTION_FREEZE, function() return Enum.ContextActionResult.Sink end, false,
            Enum.ContextActionPriority.High.Value,
            Enum.KeyCode.W, Enum.KeyCode.A, Enum.KeyCode.S, Enum.KeyCode.D, Enum.KeyCode.Space, Enum.KeyCode.LeftShift
        )
    end)

    ghostCharacter = createGhostModel(realCharacter)
    local rx, ry, _ = camera.CFrame:ToOrientation()
    pitch, yaw = rx, ry
    cameraPos = camera.CFrame.Position
    camera.CameraType = Enum.CameraType.Scriptable
    UserInputService.MouseBehavior = Enum.MouseBehavior.Default

    renderConnection = RunService.RenderStepped:Connect(function(deltaTime)
        if not isFreecamActive then return end
        freezeCharacterMovement()

        if isRightMouseDown then
            local mouseDelta = UserInputService:GetMouseDelta()
            yaw = yaw - math.rad(mouseDelta.X * CONFIG.LOOK_SENSITIVITY)
            pitch = pitch - math.rad(mouseDelta.Y * CONFIG.LOOK_SENSITIVITY)
            pitch = math.clamp(pitch, math.rad(-89), math.rad(89))
        end

        local cameraRotation = CFrame.Angles(0, yaw, 0) * CFrame.Angles(pitch, 0, 0)
        local moveVector = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVector += Vector3.new(0, 0, -1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVector += Vector3.new(0, 0, 1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVector += Vector3.new(-1, 0, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVector += Vector3.new(1, 0, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveVector += Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveVector += Vector3.new(0, -1, 0) end

        if moveVector.Magnitude > 0 and isSpectating then
            isSpectating = false
            targetPlayer = nil
            nameLabel.Text = "Free Flying"
        end

        if isSpectating and targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            cameraPos = targetPlayer.Character.HumanoidRootPart.Position - (cameraRotation.LookVector * 12) + Vector3.new(0, 3, 0)
        else
            local speed = CONFIG.MOVE_SPEED
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then speed *= 2 end
            cameraPos += (cameraRotation * moveVector) * speed * deltaTime
        end

        camera.CFrame = CFrame.new(cameraPos) * cameraRotation
    end)
end

---------------------------------------------------------
-- UI CONNECTIONS & EVENTS
---------------------------------------------------------
startButton.MouseButton1Click:Connect(function()
    if not isTeleportRunning then task.spawn(startTeleportSequence) else isTeleportRunning = false end
end)

antiMurderBtn.MouseButton1Click:Connect(function()
    isAntiMurderActive = not isAntiMurderActive
    antiMurderBtn.BackgroundColor3 = isAntiMurderActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
    updateButtonTexts()
end)

murderKAButton.MouseButton1Click:Connect(function()
    isMurderKillAuraActive = not isMurderKillAuraActive
    murderKAButton.BackgroundColor3 = isMurderKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
    updateButtonTexts()
end)

sheriffKAButton.MouseButton1Click:Connect(function()
    isSheriffKillAuraActive = not isSheriffKillAuraActive
    sheriffKAButton.BackgroundColor3 = isSheriffKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
    updateButtonTexts()
end)

prevBtn.MouseButton1Click:Connect(function() if isFreecamActive then setSpectateTarget(spectateIndex - 1) end end)
nextBtn.MouseButton1Click:Connect(function() if isFreecamActive then setSpectateTarget(spectateIndex + 1) end end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if listeningForKeyConfig then return end
    
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isRightMouseDown = true
        if isFreecamActive then UserInputService.MouseBehavior = Enum.MouseBehavior.LockCurrentPosition end
    elseif input.UserInputType == Enum.UserInputType.MouseButton3 then
        if isFreecamActive then teleportToFreecam() end
    elseif input.UserInputType == Enum.UserInputType.Keyboard and not gameProcessed then
        if input.KeyCode == CONFIG.KEY_TP_LOOP then
            if not isTeleportRunning then task.spawn(startTeleportSequence) else isTeleportRunning = false end
        elseif input.KeyCode == CONFIG.KEY_ANTI_MURDER then
            isAntiMurderActive = not isAntiMurderActive
            antiMurderBtn.BackgroundColor3 = isAntiMurderActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
            updateButtonTexts()
        elseif input.KeyCode == CONFIG.KEY_MURDER_KA then
            isMurderKillAuraActive = not isMurderKillAuraActive
            murderKAButton.BackgroundColor3 = isMurderKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
            updateButtonTexts()
        elseif input.KeyCode == CONFIG.KEY_SHERIFF_KA then
            isSheriffKillAuraActive = not isSheriffKillAuraActive
            sheriffKAButton.BackgroundColor3 = isSheriffKillAuraActive and Color3.fromRGB(20, 45, 35) or Color3.fromRGB(45, 25, 35)
            updateButtonTexts()
        elseif input.KeyCode == CONFIG.KEY_FREECAM then
            if isFreecamActive then disableFreecam() else enableFreecam() end
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isRightMouseDown = false
        if isFreecamActive then UserInputService.MouseBehavior = Enum.MouseBehavior.Default end
    end
end)

toggleButton.MouseButton1Click:Connect(function() if isFreecamActive then disableFreecam() else enableFreecam() end end)

closeButton.MouseButton1Click:Connect(function()
    isTeleportRunning = false
    isAntiMurderActive = false
    isMurderKillAuraActive = false
    isSheriffKillAuraActive = false
    disableFreecam()
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character and p.Character:FindFirstChild("RoleHighlight") then p.Character.RoleHighlight:Destroy() end
    end
    screenGui:Destroy()
end)

localPlayer.CharacterAdded:Connect(function() disableFreecam() end)
updateButtonUI()
